# SmartShapeCrop 综合形状功能可行性分析与实现报告 V2.0

**版本** V2.0 · **日期** 2026-09-23 · **分析对象** SmartShapeCrop（`F:\SmartShapeCrop`）

**本版变更**：合并 V1.0（`ProductSummary/项目审查报告/综合形状_中间挖洞+L形挖角_实现方案分析_V1.0_20260915.md`）与早期短报告，并固化三项开工决策。本报告取代上述两份文档作为唯一决策依据。

---

## 0. 已拍板决策（V2.0 固化）

| # | 决策项 | 选定方案 | 影响 |
|---|---|---|---|
| D1 | **外壳** | **方案 B：新开独立面板**（`gui/composite_panel.py` 继承 `LShapePanel`） | 需新增第 4 历史源 + `main.py` 三处装配 + 两处线程退役；UI 清晰度最好 |
| D2 | **洞填充语义** | **洞支持填素材**（不止挖空白） | `pool_hole_transparent` 在复合模式下必须可切换；需放开 `design_model.py:183` 与 `property_panel_generate.py:377` 两处守卫；阶段 2 工期 +1–2 天 |
| D3 | **洞/角组合范围** | **单洞 + 多角（1–4 角）**，暂不支持多洞+多角 | `_get_inner_pixel_mask` 复合分支走"单洞"路径，不复用多洞 UNION 逻辑；规避洞-洞-角三重交互 |

**目标功能一句话**：矩形外框 − Σ角部矩形（1–4 角）− 单个中心矩形洞；中心洞支持"挖空白"和"素材填充"两种语义；通过独立面板入口操作。

---

## 1. 需求映射与草图真值

### 1.1 示意图规格

草图标注真值为画布 185×88 cm；中心矩形洞 80×60 cm，左/右/上/下边距分别为 45/60/10/18 cm。程序输出沿用既有 1cm 损耗补偿：画布输出 186×89 cm，中心洞输出 81×61 cm，边距和挖角尺寸不变。外轮廓在一个角位形成标准单边 L 形。L 形角位允许左上（tl）、左下（bl）、右上（tr）、右下（br）。**本版支持 1–4 角**（多角），中心洞为**单洞**。

### 1.2 草图数值读数

| 标注 | 值 | 语义 |
|---|---|---|
| 185 / 88 | 185 × 88 cm | 草图外框宽 / 高 |
| 35 / 10（右上示例） | 35 × 10 cm | 角部挖角 宽 / 高（多角时每个角一组） |
| 80 / 60 | 80 × 60 cm | 草图中心洞 宽 / 高 |
| 左45 / 右60 | 45 / 60 cm | 洞的左 / 右边距 |
| 上10 / 下18 | 10 / 18 cm | 洞的上 / 下边距 |

### 1.3 数值自洽验算

```
横向：45 + 80 + 60 = 185 ✓   （草图外框宽）
纵向：10 + 60 + 18 =  88 ✓   （草图外框高）
```

程序输出补偿后的自洽关系为：`45 + 81 + 60 = 186`、`10 + 61 + 18 = 89`。

> 这是复合形状相比纯 L 形的关键优势：多出 2 个**强等式约束**（`ml + hole_w + mr == outer_w` ∧ `mt + hole_h + mb == outer_h`），OCR 误识更易被检出并回退到几何值，直接降低草图识别阶段风险。

### 1.4 形状公式

```
KEEP = Rect(outer_w × outer_h) − Σ CutRect(corner, w, h) − Hole(hole_w × hole_h, 带圆角)
```

| 区域 | x 范围 | y 范围 |
|---|---|---|
| 外框 | 0 – 185 | 0 – 88 |
| L 挖角（示例 tr） | 150 – 185 | 0 – 10 |
| 中心洞 | 45 – 125 | 10 – 70 |

**两个直接影响实现的几何事实**

1. **洞与挖角不相交、也不相邻**——挖角下沿 `y=10` 与洞上沿 `y=10` 恰好共线，但 x 区间相隔 25 cm 实心带。POC 实测：重叠 0 像素，1px 膨胀后相邻像素仍为 0。→ 10px 黑框必须**分形状各自绘制后 OR 合并**，**不能**用"整体 union 再内缩"。
2. **挖角整体落在右边距环带内**（挖角 x 起点 150 > 洞右边界 125）。→ 挖角只从"外框环带"上削掉一块，不侵入洞附近材料，几何耦合度低。

### 1.5 与现有三种模式的关系

| 现有模式 | 几何操作 | 与综合形状的关系 |
|---|---|---|
| `rect_hole` | 矩形 − 中间洞 | 提供 **洞** 这一半 |
| `rect_lshape` | 矩形 − Σ角部矩形（≤4） | 提供 **挖角** 这一半 |
| `ellipse_hole` | 矩形 − 椭圆 | 无关，独立入口 |
| **`rect_lshape_hole`（新增）** | 矩形 − Σ角部矩形 − 单中间洞 | = 前两者的**组合** |

---

## 2. 可行性结论

**可行。** 已有零改动 POC 实测证实：在现有几何原语上纯组合即可实现，集合代数 4 项恒等式精确成立（见 §10）。

**但"可行"不等于"零改动"**。早期短报告曾主张"无需更换数据模型、现有实现可直接复用"——该判断不成立，原因如下（详见 §12 修正说明）：

1. `mode` 是互斥枚举（[geometry.py:162](file:///f:/SmartShapeCrop/core/geometry.py#L162)、[:274](file:///f:/SmartShapeCrop/core/geometry.py#L274)），渲染入口是 `if/elif/else` 三分派（[image_ops.py:1557](file:///f:/SmartShapeCrop/core/image_ops.py#L1557)），**物理上不可能**靠"同时设两个 mode"实现复合。
2. 历史 `rect_lshape` 语义是"外框与内洞**共用 L 形掩膜**"（[design_model.py:183-184](file:///f:/SmartShapeCrop/models/design_model.py#L183) 强制 `pool_hole_transparent=True`），不是示意图要的"外 L + 中矩形洞"。
3. 全工程 19 处 `mode ==/!= 'rect_lshape'` 硬判断（含 3 处 `!=`，仅搜 `==` 会漏），必须四类分治而非批量替换。

**正确路径**：新增 `mode='rect_lshape_hole'` + `build_composite_mask()` + 19 处四类分治 + 新 `composite_sketch_parser.py` + G1 闸口扩展 + 独立面板（D1）。预计 **10–16 个工作日**（含 D2 洞填素材 +1–2 天、D1 独立面板 +1–2 天）。

---

## 3. 现有架构盘点

### 3.1 已具备、可直接复用的资产

| 能力 | 位置 | 复合模式能直接用的部分 |
|---|---|---|
| 多角 L 形 mask | `core/geometry.py` `build_lshape_mask(..., cuts=[...])` | **原生支持 1–4 角**，签名已含 `cuts` |
| 多角参数模型 | `l_cuts_cm` + `l_shapes_px()` | 数据结构现成，最多 4 角、禁重复角位 |
| 洞 mask | [image_ops.py:1522](file:///f:/SmartShapeCrop/core/image_ops.py#L1522) `fill_rect_mask` | 单洞逻辑现成（D3 只需单洞） |
| 洞的 10px 黑框环带 | "full & ~shrunk" 环带算法 | 现成，可直接复用到洞 |
| L 形切边 10px 黑框 | [image_ops.py:1169](file:///f:/SmartShapeCrop/core/image_ops.py#L1169) | 逐 cut 的"L 形 & ~收缩 L 形"算法现成 |
| L 形素材边框补全 | `core/lshape_border.py` + `lshape_border_route.py` | 逐角调度 + 相邻角冲突策略已完成（F-1 最大前置债务已还清） |
| L 形草图几何识别 | `services/sketch_parser/lshape_sketch_parser.py` | `RETR_EXTERNAL` 取最大外轮廓 → 中间有洞不影响 |
| 多角 UI + 实时边余量提示 | `gui/lshape_panel.py` | 现成，可继承 |
| G1/G2/G3 闸口 | 识别层 + `gui/lshape_panel.py` + `canvas_widget.py` | 现成"识别不完整必须报警"防线 |
| 后台线程 / 打包内嵌 Tesseract | `workers/`、`packaging/packageV2.2.2.py` | 零改动 |

**结论：约 85% 的管线可复用。真正要新写的只有"把两块 mask 组合起来 + 让草图多认几个数 + 独立面板外壳"。**

### 3.2 阻塞点：`mode` 是互斥枚举

```python
# core/geometry.py:162
mode: Literal['rect_hole', 'rect_lshape', 'ellipse_hole'] = 'rect_hole'
# core/geometry.py:274
_VALID_MODES = frozenset({'rect_hole', 'rect_lshape', 'ellipse_hole'})

# core/image_ops.py:1551-1557  _get_inner_pixel_mask() 分派
if design.mode == 'rect_hole':     ... return
elif design.mode == 'rect_lshape': ... return
else:  # ellipse_hole             ... return
```

### 3.3 硬判断分布（19 处），必须四类分治

`mode == / != 'rect_lshape'` 全工程共 **19 处**（逐行实测，含 3 处 `!=`）：

| 文件 | 处数 | 当前行号 |
|---|---|---|
| `core/image_ops.py` | 9 | 764, 782, 802, 899, 1075, 1093, 1169, 1216, 1235, 1557 |
| `gui/property_panel_generate.py` | 6 | 225, 241, 291, 377, 532, 575 |
| `core/geometry.py` | 2 | 308, 679 |
| `models/design_model.py` | 2 | 114, 183 |

> ⚠️ 其中 3 处是 `!=`（`design_model.py:114`、`property_panel_generate.py:241`、`:377`）。只搜 `== 'rect_lshape'` 会**漏掉这 3 处**——这是本项目历史上"改了这个坏那个"的典型成因。

**四类分治（绝不能无脑全局替换）：**

**类 ① 渲染语义 —— 改为 `mode in LSHAPE_LAYOUT_MODES`（8 处，全在 `image_ops.py`）**

| 行号 | 作用 |
|---|---|
| 764 | 非 L 形区域填 `outer_bg_color` |
| 782 | L 形 + 外背景图时跳过边框带着色 |
| 802 | L 形 + 池素材渲染分支 |
| 899 | cut_area_mask 返回条件 |
| 1075 | L 形 cut 未完成时的填色 |
| 1093 | cut 区保持外背景色 |
| 1169 | L 形切边 10px 黑框（收缩 L 形差集） |
| 1216 / 1235 | L 形 + 池素材时跳过统一黑框 / 触发素材边框补全 |

**类 ② 分派点 —— 必须新增独立分支，不能改成 membership 判断（2 处）**

| 位置 | 说明 |
|---|---|
| [image_ops.py:1557](file:///f:/SmartShapeCrop/core/image_ops.py#L1557) | `_get_inner_pixel_mask()` 的 `elif`——复合模式不是"也是 L 形"，而是"L 形 **减去** 洞"，需要独立的 mask 组合逻辑 |
| `core/geometry.py:679` `compute_border_bands()` | 复合模式的边框带 = L 形带 ∪ 洞环带 |

> 若把这两处误改成 `in LSHAPE_LAYOUT_MODES`，会得到"L 形 + 洞"的**并集**（洞被填满）而不是**差集**，且不会报错。

**类 ③ 展示与 UI 同步 —— 需扩展（4 处，`property_panel_generate.py`）**

| 行号 | 作用 | 处理 |
|---|---|---|
| 225 | L 形从 `LShapePanel` 推算画布尺寸 | 扩展（复合模式同样需要） |
| 291 | 回填 `LShapePanel`（corner/cut/外框） | 扩展 |
| 532 | 成功对话框文案（L 形参数行） | 扩展（追加洞信息行） |
| 575 | 成功对话框文案（识别结果行） | 扩展 |

> 这 4 处最容易被误判为「类 ④」而漏改。它们长得像参数守卫，实际只是展示/同步。

**类 ④ 参数语义守卫 —— 不能扩展，需重写逻辑（5 处）**

| 位置 | 现有逻辑 | 为何不能扩展 / 要改成什么 |
|---|---|---|
| [design_model.py:114](file:///f:/SmartShapeCrop/models/design_model.py#L114) | `if d.mode != 'rect_lshape'` 才写 inner_margin | L 形语义下边距固定为 0；**复合模式下边距恰恰是洞的定义** → 改为"仅 `rect_lshape` 跳过" |
| [design_model.py:183](file:///f:/SmartShapeCrop/models/design_model.py#L183) | `if d.mode == 'rect_lshape': pool_hole_transparent = True` | **D2 决策点**：复合模式洞要支持填素材 → 复合模式走洞的 blank/image 分支，**不能强制 True** |
| `property_panel_generate.py:241` | `if mode != 'rect_lshape'` 才写 inner_margin | 同 `design_model.py:114` |
| [property_panel_generate.py:377](file:///f:/SmartShapeCrop/gui/property_panel_generate.py#L377) | `if hm == "image" and mode != 'rect_lshape'` 才启动内挖素材匹配 | **D2 决策点**：复合模式的洞**也要能填素材** → 需放开 |
| `core/geometry.py:308` `validate()` | L 形专项校验 | 需新增复合分支；**挖角约束的基准矩形要从 `inner_rect` 改为 `outer_rect`** |

> **执行建议**：为这 19 处逐条加注释 `# [COMPOSITE] 类①/②/③/④`，再逐条改写。不要做字符串级批量替换。

### 3.4 隐藏耦合（最容易踩的坑）

| 位置 | 问题 | 后果 |
|---|---|---|
| [property_panel.py:927](file:///f:/SmartShapeCrop/gui/property_panel.py#L927) | **硬编码索引映射** `{'rect_hole': 0, 'rect_lshape': 1, 'ellipse_hole': 2}.get(d.mode, 0)` | 不同步 → 模板加载**静默回落 `rect_hole`**，形状错误且不报错 |
| [property_panel.py:135-138](file:///f:/SmartShapeCrop/gui/property_panel.py#L135) | `_cb_mode` 只有 3 个 `addItem` | 需追加第 4 项 |
| [property_panel.py:560](file:///f:/SmartShapeCrop/gui/property_panel.py#L560) | `findData('rect_lshape')` 自动切模式路径 | 复合模式需要自己的入口 |
| [app_settings.py:318-319](file:///f:/SmartShapeCrop/core/app_settings.py#L318) | `if src not in (CROPPER, POOL, LSHAPE): src = CROPPER` —— **未知 source 静默回落 cropper** | **D1 决策点**：新增第 4 源必须同步白名单，否则历史记录**静默写进圆角裁剪工具的历史** |
| `core/geometry.py:308-324` `validate()` | 逐边约束基于 `inner_rect` | 复合模式下基准矩形应为 `outer_rect` |
| `main.py` | 4 个 Tab 装配点 + `closeEvent` 与 `aboutToQuit` **两处**线程退役 | **D1 决策点**：漏接 → 关窗时 `QThread: Destroyed while thread is still running` 崩溃（N-P0-02 历史教训） |

---

## 4. 方案对比与外壳选型（D1 已选定 B）

| 维度 | A. 新建独立项目 | **B. 现有项目 + 新增第 4 Tab 面板（继承 LShapePanel）** ✅ 选定 | C. L 面板内加「同时中间挖洞」勾选 | D. 塞进水池面板 |
|---|---|---|---|---|
| 复用率 | 0% | ~85% | ~85% | ~85% |
| 代码维护 | 永久双份 | 单份 | 单份 | 单份 |
| 对现网回归风险 | 无 | 低 | 最低 | 高（水池面板已过载） |
| 新增历史源 | 需全套 | 需第 4 源（须同步 `app_settings` 白名单） | 不需要 | 不需要 |
| 新增 Tab / 线程退役点 | — | 需（**两处**） | 不需要 | 不需要 |
| UI 清晰度 | 最好 | 好 | 一般（L 面板 1036 行再加 7 个洞控件偏挤） | 差 |
| 复合草图 10 标注语义契合度 | — | 好（独立入口，G1/G2/G3 闸口不必双模态化） | 一般 | 差 |
| **结论** | ❌ 否决 | ✅ **选定** | （被 D1 取代） | ❌ 否决 |

### 4.1 为什么选 B 而非 C

D1 已选定 B。相比 C（L 面板内加勾选），B 的代价是多 1 天外壳工作 + 线程退役接线，但收益是：

1. **UI 清晰度**：复合草图有 10 个标注，独立入口避免 L 面板（已 1036 行）进一步拥挤。
2. **G1 闸口不必双模态化**：独立面板下识别分支无需在"L-only / L+洞"之间分叉。
3. **继承而非复制**：`composite_panel.py` 继承 `LShapePanel`，不复制脚手架，避免水池面板"寄生 + 多链路穿透"的反模式。
4. **引擎完全相同**：B 与 C 共用同一个 `mode`、同一套几何组合、同一套渲染分支。外壳选择不动引擎。

### 4.2 否决 A 与 D

- **A（新建项目）**：图形引擎（`geometry.py`+`image_ops.py`+`lshape_border*.py` ≈ 4500 行纯函数）+ OCR + Tesseract 内嵌打包全是重资产，100% 重复 + 永久双份维护。L 形边框补全三级路由已被真实案例反复打磨，任何副本会立刻漂移。
- **D（水池面板）**：水池面板已 991+1043 行、职责过载；"洞参数寄生在水池面板、被多条链路穿透读写"已被列为最大难度来源。再塞 L 形等于制造第三个穿透点。

---

## 5. 洞填充语义（D2：支持空白 + 素材填充）

| 模式 | 字段设置 | 渲染效果 |
|---|---|---|
| **挖空白** | `pool_hole_transparent=True` | 中心洞和 L 缺口以纯白表达，适合 JPG 输出的挖空语义 |
| **素材填充** | `pool_hole_transparent=False` + `pool_inner_material_image=<素材>` | 中心洞按洞矩形尺寸适配素材；若未提供素材则用 `hole_bg_color` |
| L 形外框素材 | 始终保留 | 保留区始终是外框素材，缺口区域按上述空白/填充语义处理 |

### 5.1 D2 对代码守卫的影响（关键）

现有两处守卫会**阻止**复合模式洞填素材，必须改写（属 §3.3 类 ④）：

```python
# models/design_model.py:183  —— 现状：L 形强制挖空白
if d.mode == 'rect_lshape':
    d.pool_hole_transparent = True       # ← 复合模式不能走这里
# 改为：复合模式走洞的 blank/image 分支（与 rect_hole 一致）

# gui/property_panel_generate.py:377  —— 现状：非 L 形才启动内挖素材匹配
if hm == "image" and self.design.mode != 'rect_lshape':   # ← 复合模式被挡
# 改为：复合模式也允许启动内挖素材匹配
```

> D2 使阶段 2 工期 +1–2 天（需处理复合模式下素材适配 + 边框层次共存）。

---

## 6. 推荐落地路线（6 阶段，已纳入 D1/D2/D3）

> **贯穿全程的硬约束**：每一阶段结束时，现有 3 个模式行为必须 100% 不变。

### 6.0 当前落地状态（2026-09-26 复核）

| 阶段 | 状态 | 已落地内容 | 尚未完成内容 |
|---|---|---|---|
| 阶段 1 几何层 | **基本完成** | 新模式注册、`build_composite_mask()`、外框基准校验、复合边框带路由、几何单元测试。 | 仍需接入最终面板字段并完成端到端回归。 |
| 阶段 2 渲染管线 | **基本完成** | 复合模式中心洞分派、洞素材填充、L 形挖角独立处理、洞边框与挖角边框合并、基础渲染测试。 | 仍需真实素材组合回归和旧三模式完整像素级回归；L 形素材边框补全暂沿用旧模式专用路径。 |
| 阶段 3 草图识别 | **已接入统一解析分派，仍需完善** | 新增 `composite_sketch_parser.py`、`parse_shape_sketch(mode='rect_lshape_hole')`、单洞轮廓几何换算、G1 洞字段、自洽校验、1cm 输出损耗补偿；旧模式继续走原解析器。 | 尚未完成 OCR 十类角色映射、`RETR_CCOMP/RETR_TREE` 内轮廓策略、真实草图回归和 GUI 回填。 |
| 阶段 4 独立面板 | **已接入主窗口与独立 Worker** | `CompositePanel` 已接入 `main.py` 第 4 个 Tab；新增 `_CompositeParseWorker`；识别按钮后台调用统一 `parse_shape_sketch()` 并回填洞参数；关闭窗口和 `aboutToQuit` 均接入线程退役。 | 尚未接入复合模式历史源、主设计模型最终回填和真实草图 GUI 回归。 |

因此，阶段 1 可标记为“基本完成”，阶段 2 和阶段 3 不应标记为“完全完成”。

### 阶段 1 —— 几何层（`core/geometry.py`）· 0.5–1 天
- 新增 `'rect_lshape_hole'` 到 `Literal` 与 `_VALID_MODES`
- 新增 `build_composite_mask(...)`：`L形(outer_rect, cuts) − 洞(hole_rect, 带圆角)`（**单洞**，D3）
- `validate()` 新增复合分支（挖角约束按 **`outer_rect`** 计算；洞边距沿用现有逻辑）
- `compute_border_bands()` 新增复合路由
- **验收**：§10 的 4 项集合代数恒等式全 PASS + `validate()` 正/反例

### 阶段 2 —— 渲染管线（`core/image_ops.py`）· 4–7 天（含 D2 +1–2 天）【基本完成】
- `_get_inner_pixel_mask()` 新增复合分支：中心洞作为独立填充区域，角部 cut 由独立 L 形路径处理
- 类 ① 的 **8 处**渲染语义硬判断改为 `design.mode in LSHAPE_LAYOUT_MODES`
- 类 ② 的 **2 处分派点**（`:1557`、`geometry.py:679`）新增独立复合分支
- `_compute_border_mask()` 复合分支：`border = L形切边环带 ∪ 洞环带`（分形状 OR 合并，不做联合内缩）
- **D2**：放开 `design_model.py:183` 与 `property_panel_generate.py:377`，使洞支持素材填充
- `_lshape_border_completion()` 触发条件纳入复合模式（**cuts 不得混入洞矩形**，见 R5/C3）
- **已完成**：洞素材填充、洞边框与角部边框分开计算后合并；新增复合渲染测试
- **待验收**：真实素材组合回归；3 个旧模式黄金样本**像素级**比对无差异

### 阶段 3 —— 草图识别（`services/sketch_parser/`）· 3–5 天【已接入统一解析分派，识别增强待完成】
- 新增 `composite_sketch_parser.py`
  - **可复用**：`_extract_largest_contour`（`RETR_EXTERNAL` → 中间有洞不影响）、`_detect_concave_sliding_window`、`_detect_by_convex_hull`、`_finalize_lshape_geometry`
  - **需新增**：洞的内轮廓提取（`RETR_CCOMP` / `RETR_TREE`）
  - **需新增**：10 标注角色映射（外框2 + 挖角2×N角 + 洞2 + 边距4）
  - **需新增**：复合自洽校验 `ml + hole_w + mr == outer_w`、`mt + hole_h + mb == outer_h`
- 外框真值始终信任目标文件名（沿用 `parse_lshape_sketch` 既有不变量）
- **验收**：对真实案例 10 个数值误差 ≤ 0.5 cm；OCR 不可用时几何降级不崩溃

### 阶段 4 —— 外壳（D1：方案 B）· 2–3 天【已接入主窗口与独立 Worker】
- 新建 `gui/composite_panel.py` **继承 `LShapePanel()`**，覆写 `get_lshape_params()`，追加洞参数控件（宽高 + 四边距 + 挖空方式 blank/image，D2）
- `main.py` 三处装配 + `closeEvent`/`aboutToQuit` **两处**线程退役（N-P0-02 教训）
- `gui/property_panel.py`：`_cb_mode` 追加第 4 项（[:135-138](file:///f:/SmartShapeCrop/gui/property_panel.py#L135)）；**[:927] 硬编码映射改为 `_cb_mode.findData(mode)`**
- `core/app_settings.py`：新增第 4 历史源 `TARGET_SRC_COMPOSITE` + 同步 `TARGET_SRC_LABEL` 与白名单（[:318](file:///f:/SmartShapeCrop/core/app_settings.py#L318)），**避开静默回落 cropper 陷阱**
- `models/design_model.py`：新增复合分支（inner_margin 必须允许写入；pool_hole_transparent 按 D2 可切换）
- **验收**：离屏 `QT_QPA_PLATFORM=offscreen` 面板构造冒烟 + 信号契约测试

### 阶段 5 —— 回归与打包 · 1–2 天
- 新增 `tests/core/test_composite_shape.py`（几何 + 校验）、`tests/integration/test_composite_flow.py`（端到端，含 D2 素材填充用例）
- 全量测试回归 + 旧 3 模式像素级比对
- `packaging/packageV2.2.2.py` 与 spec 补 hidden import；**核对 `dist/*.exe` 时间戳 ≥ 最新源码时间戳**

### 阶段 6 —— 文档同步 · 0.5 天
- `README.md`：架构概览 / 目录结构 / 核心模块说明 / 快速开始（测试数量）
- `L形挖角已知问题与后续规划.md`：F-1 标记进度、SR-3 标记化解

### 工作量汇总

| 阶段 | 预估 | 说明 |
|---|---|---|
| 1 几何层 | 0.5–1 天 | ~120 行 |
| 2 渲染管线 | 4–7 天 | ~150 行 + 8 处语义扩展 + D2 守卫改写 |
| 3 草图识别 | 3–5 天 | ~350 行 |
| 4 外壳（B） | 2–3 天 | ~200–280 行（继承 LShapePanel + main.py 接线 + 第 4 源） |
| 5 回归 + 打包 | 1–2 天 | |
| 6 文档 | 0.5 天 | |
| **合计** | **11–18.5 天** | 含 D1 独立面板 + D2 洞填素材；D3 单洞简化已计入 |

> 优于 F-1 原估 3–4 周（多角基础设施已落地），但远高于"3 天"量级——洞与 L 形边框补全的共存交互是新工作，无现成实现可抄。

---

## 7. 关键实现要点（签名级）

### 7.1 几何层

```python
# core/geometry.py
COMPOSITE_MODE = 'rect_lshape_hole'
LSHAPE_LAYOUT_MODES = frozenset({'rect_lshape', COMPOSITE_MODE})   # 类① 判断统一入口

def build_composite_mask(
    size: tuple[int, int],
    outer_rect: RectShape,                    # L 形板 = 整幅画布（不是 inner_rect！）
    cuts: list[tuple[str, float, float]],     # 1–4 角（D3：多角）
    hole_rect: RectShape,                     # 单洞（D3：不支持多洞）
    hole_corners: dict[str, float],
    outer_corners: dict[str, float],
) -> np.ndarray:
    """KEEP = L形(outer_rect, cuts) − 洞(hole_rect, 带圆角)，返回 bool mask。"""
```

> ⚠️ **最易写错**：现有 `rect_lshape` 把 `inner_rect` 当作 L 形外轮廓（边距被强制为 0，两者相等）。复合模式下 **L 形外轮廓必须是 `outer_rect`**，`inner_rect` 改为表示洞。**两处基准矩形互换**是本次改造最易出错的地方。推荐为复合模式新增独立 `hole_rect` 字段而非重载 `inner_rect`，避免旧路径污染。

### 7.2 渲染管线

```python
# core/image_ops.py  _get_inner_pixel_mask()  复合分支（D3 单洞）
elif design.mode == 'rect_lshape_hole':
    cut_mask  = ~build_lshape_mask(size, outer_rect, cuts, ...)   # 挖掉的角
    hole_mask = fill_rect_mask(hole_rect, hole_corners)           # 单洞
    return cut_mask | hole_mask          # 「被移除区域」= 挖角 ∪ 洞
```

```python
# core/image_ops.py  _compute_border_mask()  复合分支
# 两区域不相交（§1.4 已证）→ 分形状环带 OR 合并，不做联合内缩
border = lshape_edge_band(outer_rect, cuts, bw)   # 复用现有算法
       | hole_ring(hole_rect, bw)                 # 复用现有算法
```

### 7.3 `mode` 判断收敛（防漏改）

```python
# core/geometry.py
def is_lshape_layout(mode: str) -> bool:
    """L 形渲染语义（类①）——8 处渲染判断统一走这里。"""
    return mode in LSHAPE_LAYOUT_MODES
```

> 现状 19 处裸字符串比较（3 处 `!=`，只搜 `==` 会漏），漏改一处 = 难以定位的视觉 Bug。收敛为单一来源后，后续再加形状只需改 `LSHAPE_LAYOUT_MODES` **一处**。

### 7.4 外壳（D1：方案 B）

```python
# gui/composite_panel.py
class CompositePanel(LShapePanel):
    """继承 LShapePanel，追加单洞参数控件（D2：blank/image 双模式）。"""
    def get_lshape_params(self) -> dict:
        params = super().get_lshape_params()       # 复用四角 + 外框
        params['mode'] = 'rect_lshape_hole'
        params['hole'] = {
            'w_cm': self._sp_hole_w.value(),
            'h_cm': self._sp_hole_h.value(),
            'mt': self._sp_hole_mt.value(), 'mb': self._sp_hole_mb.value(),
            'ml': self._sp_hole_ml.value(), 'mr': self._sp_hole_mr.value(),
            'fill_mode': 'image' if self._ck_hole_material.isChecked() else 'blank',
        }
        return params
```

### 7.5 草图识别

```
可复用（外轮廓从未变过）：
  _extract_largest_contour        RETR_EXTERNAL → 中间有洞不影响
  _detect_concave_sliding_window  滑动窗凹角检测（已按 bbox 四角分桶，支持多角）
  _detect_by_convex_hull          凸包差法（已返回全部合格缺口）
  _finalize_lshape_geometry       多候选消歧 + 三级硬约束
需新增：
  内轮廓提取（洞）→ RETR_CCOMP / RETR_TREE（单洞，D3）
  10 标注角色映射（外框2 + 挖角2×N + 洞2 + 边距4）
  复合自洽校验：ml + hole_w + mr == outer_w  ∧  mt + hole_h + mb == outer_h
```

> **G1 闸口必须扩展到复合模式**：现有 G1 只断言"检测到的凹角数 == 消费的凹角数"。复合模式下可能"挖角全对、洞被静默丢弃却报成功"。**必须为洞新增一条独立的结构断言**（`hole_detected` / `hole_consumed`）。

---

## 8. 风险清单与规避

### 8.1 通用风险

| # | 风险 | 等级 | 规避 |
|---|---|---|---|
| R1 | **19 处 mode 判断漏改**（含 3 处 `!=`） | 🔴 高 | 逐条加 `# [COMPOSITE] 类①②③④` 注释；类 ① 收敛为 `is_lshape_layout()`；**禁止字符串级批量替换** |
| R2 | `property_panel.py:927` 硬编码索引映射未同步 | 🔴 高 | 改为 `_cb_mode.findData(mode)`；补模板加载测试 |
| R3 | **基准矩形互换**：L 形外轮廓应用 `outer_rect` 而非 `inner_rect` | 🔴 高 | `build_composite_mask` docstring 显著标注；POC 像素断言覆盖；推荐新增 `hole_rect` 字段 |
| R4 | `_render_lshape_cut` 差集公式在复合模式失效 | 🔴 高 | 复合分支直接使用 `cut_mask`，**不复用**差集公式 |
| R5 | 10px 黑框误用"整体 union 内缩" | 🟠 中 | §1.4 已证实两区域不相交 → 强制"分形状环带 OR 合并" |
| R6 | `app_settings.py:318` 未知 source 静默回落 cropper | 🔴 高 | **D1 必改**：新增 `TARGET_SRC_COMPOSITE` 并同步 `TARGET_SRC_LABEL` + 白名单 |
| R7 | 新面板漏接线程退役 | 🔴 高 | `closeEvent` + `aboutToQuit` **两处**都要接（N-P0-02 教训） |
| R8 | 旧 3 模式回归 | 🟠 中 | 建黄金样本像素级比对 |
| R9 | 交付物落后于源码 | 🟠 中 | 打包后核对 `dist/*.exe` 时间戳 |

### 8.2 复合形状特有风险

| # | 风险 | 等级 | 说明与规避 |
|---|---|---|---|
| **C1** | **G1 闸口复合盲区：挖角对、洞漏识却报成功** | 🔴 高 | 必须为洞新增独立结构断言 `hole_detected`/`hole_consumed`。洞比角更显眼，静默失效代价更大 |
| **C2** | 洞 10px 黑框与 L 切边黑框在 `y=10` 共线 | 🟠 中 | §1.4 已证不相邻（x 相隔 25cm），但共线需检查该高度无重复加厚 |
| **C3** | L 形素材边框补全误把洞边缘当"新切边" | 🟠 中 | `apply_lshape_border_completion` 入参只传 `cuts`，**不得把洞矩形混入 cut 列表** |
| **C4** | 复合草图标注密集度提升 → OCR 误归属放大 | 🟠 中 | 识别只给建议值，尺寸交用户就地修正；§1.3 两等式作额外纠错闸口 |
| **C5** | **D2 洞填素材时，洞内素材与 L 外框素材的边框层次共存** | 🟠 中 | 需测试"洞填素材 + 外框素材 + 10px 黑框"三者的渲染顺序与遮挡；阶段 2 验收必含此用例 |

---

## 9. 与既有文档/规划的关系

| 既有文档 | 原有结论 | 与本报告关系 |
|---|---|---|
| `L形挖角已知问题与后续规划.md` **F-1**（P2，3–4 周） | 「多 L 形挖角 + 中间洞复合支持」 | 本报告 = F-1 的开工版：工期下修到 11–18.5 天，D1/D2/D3 已固化 |
| 同上 **SR-3** | 「单 L 形无法复合」 | 本报告即 SR-3 的化解方案 |
| `SmartShapeCrop-多角L形挖角可行性分析 V1.1` §8 | 建议扩展现有 L 面板，不要单开 | 该结论针对"多角 vs 单角"（参数扩展）；本问题针对"L + 洞"（新几何实体），前提不同。D1 选 B 不与 V1.1 §8 矛盾，因前提已变 |

---

## 10. 零改动可行性验证（已执行）

为在动手前拿到证据，写了一个**不修改任何生产源码**的诊断脚本，仅用现有原语合成复合 mask：

- **脚本**：`scripts/diagnose/_diag_composite_shape_poc.py`（未追踪新文件，不进 CI）
- **产物**：`_archive/poc_composite/composite_mask_poc.png`
- **运行**：`F:\SmartShapeCrop\.venv\Scripts\python.exe scripts/diagnose/_diag_composite_shape_poc.py`

### 10.1 实测输出

```
canvas = 9300 x 4450 px @ 127 dpi  (186.0 x 89.0 cm)
外框面积     =  16280.0 cm^2   (期望 16280.0)
L挖角面积    =    350.7 cm^2   (期望   350.0)
中间洞面积   =   4802.8 cm^2   (期望  4800.0)
保留材料面积 =  11126.5 cm^2   (期望 11130.0)

洞与挖角相邻像素(1px 膨胀) = 0   （0 = 无共边，10px 黑框可分别绘制后 OR 合并）
面积相对误差 = 0.031%           （仅来自 PIL 矩形 1px 边界栅格化，非几何误差）

PASS  L形 + 挖角 == 整幅画布
PASS  保留区 ∩ 洞 == 空
PASS  保留区 ∪ 洞 == L形（恰好划分）
PASS  洞 ∩ 挖角 == 空（两区域不相交）
```

### 10.2 验证结论

1. **集合代数 4 项恒等式全部精确成立** —— 复合形状可纯组合实现，无需新写几何算法，印证 F-1 原定"布尔 `Σ CutRect` 差集"方向正确。
2. **面积误差 0.031%** 完全来自 `PIL.ImageDraw.rectangle` 1px 边界包含行为，与几何无关。
3. **洞与挖角不相交、不相邻** —— 证实 §1.4 判断，10px 黑框可分形状绘制后 OR 合并。
4. 该脚本**未导入、未修改任何 `gui`/`workers`/`services` 模块**，只读调用 `core.geometry` 公开函数。

> 这份 POC 可直接作为阶段 1 的回归基线：实现 `build_composite_mask()` 后把脚本切换过去，4 项断言必须仍全 PASS。

---

## 11. 验收标准

### 11.1 回归基线（硬指标）

- **全量测试必须持平**：基线 `671 passed`（2026-09-19，提升权限，`$env:PYTHONDONTWRITEBYTECODE='1'; python -m pytest tests/ -q -p no:cacheprovider`）。普通权限下临时目录权限问题会导致 ~45 个环境性 error，非功能性失败。
- **旧 3 模式零退化**：`rect_hole` / `rect_lshape` / `ellipse_hole` 黄金样本输出**像素级**一致。
- **改动面证据**：阶段 1–3 期间对 `gui`、`workers` 的 `git diff --stat` 应为空。

### 11.2 端到端验收清单

- [ ] 上传综合草图 → 识别出 10 个数值，误差 ≤ 0.5 cm；两个自洽等式成立
- [ ] 生成预览：中间是洞（白底/素材 + 四周 10px 黑框），角部是 L 形挖角（白底 + 沿切边有素材边框层次）
- [ ] **D2**：洞切换为"素材填充"→ 洞内显示适配素材；切回"挖空白"→ 洞显示纯白
- [ ] **D3**：设置 1–4 个角部挖角 + 单洞 → 全部正确渲染；不提供多洞入口
- [ ] **G1 扩展断言生效**：人为构造"挖角对、洞漏识"的草图 → 必须降级为"部分完成"，不得报成功
- [ ] 手动改任一边距 / 挖角尺寸 → 重生成形状同步变化、无残留边框
- [ ] 导出 JPG 尺寸 = 设计尺寸 + 1cm 损耗，DPI 元数据正确
- [ ] 切回原有 3 个模式，行为与现网一致
- [ ] **D1**：独立面板 Tab 可正常切换；关窗无 `QThread: Destroyed` 崩溃；历史记录写入第 4 源而非 cropper
- [ ] 打包 exe 双击可运行、OCR 可用（Tesseract 已内嵌）

---

## 12. 对早期短报告的修正说明（分析说清楚）

早期短报告（本文件 V1.0 版本）曾得出"现有能力内可行、无需换数据模型"的结论。经逐条核实，该结论**不成立且具误导性**，原因如下：

| 短报告主张 | 判断 | 核实证据 |
|---|---|---|
| "无需更换数据模型…现有实现可直接复用" | ❌ 不成立 | 短报告自身 §5 承认需新增 `inner_hole_shape` 字段、改 5 处；实测为 19 处 mode 判断 + 新 mode + 新 parser。`mode` 是互斥枚举（[geometry.py:162](file:///f:/SmartShapeCrop/core/geometry.py#L162)），"不换数据模型"在物理上无法表达复合 |
| 列举 `build_lshape_mask`/`render_design` 等"已具备" | ⚠️ 偷换语义 | 这些原语服务于历史 `rect_lshape`（**外框与内洞共用 L 掩膜**），其 `inner_rect` 是 L 外轮廓；复合模式要求 `inner_rect` 表洞，**基准矩形互换**是最易错点 |
| "24 passed"测试 | ❌ 证据错配 | 24 个测试全部是 `mode='rect_lshape'` 既有行为测试（[test_pool_lshape_flow.py:38](file:///f:/SmartShapeCrop/tests/integration/test_pool_lshape_flow.py#L38)），验证"挖角+池素材"，非"挖角+矩形中洞"。复合模式尚无任何测试 |
| "属于下一步增强，不应隐式改变现有输出" | ⚠️ 定性错误 | 这不是"增强"，而是实现示意图目标的**必要条件**。降级为可选增强等于承认"可行"指的是另一个功能 |

**为什么仍"可行"**：POC（§10）已证集合代数 4 恒等式精确成立，现有几何原语可纯组合实现。可行 ≠ 零改动；正确表述是"可行，需新 mode + 19 处分治 + 独立面板，11–18.5 天"。

---

## 附录 A · 草图数值验算明细

```
外框 186 × 89 cm

横向：左45 + 洞81 + 右60 = 186  ✓
纵向：上10 + 洞61 + 下18 =  89  ✓

右上挖角 35(宽) × 10(高)
  挖角 x 区间 = [185-35, 185] = [150, 185]     y 区间 = [0, 10]
  洞   x 区间 = [45, 125]                       y 区间 = [10, 70]

→ 挖角与洞在 x 方向间隔 25 cm（不相交、不相邻）✓
→ 挖角下沿 y=10 与洞上沿 y=10 恰好共线（实现时注意该高度上两套黑框不要互相加厚）
```

## 附录 B · 本项目已踩过的坑（本次改造须避免重蹈）

1. **`git gc` / `repack` 在本机不零风险** —— 曾导致 `.git` 被清空。改动 `.git` 文件系统前先 `cp -r .git .git.bak`。
2. **`.gitignore` 对已追踪文件无效** —— 清理必须配套 `git rm --cached`，否则下次 checkout 会"复活"。
3. **"已删除"不是终态** —— `ProductSummary/2026-*` 日期目录曾整批重现。目录治理必须落到索引层。
4. **缓存失效要校验来源** —— `_cached_outer_image` 曾因不校验 `_cached_outer_src` 导致"换素材后画面仍是旧素材"反复复发 5 次以上。
5. **静默失效比崩溃危险** —— V1.1 实测：识别漏掉一个挖角却报告"识别成功"。复合模式必须扩展 G1 闸口（C1）。
6. **交付物时间戳必须核验** —— 曾出现 `dist/*.exe`(14:22) 落后于 `main.py`(15:11)。
7. **`python-qt5` 黑包会破坏 PyQt5 DLL 加载** —— 禁止安装（AGENTS.md 明令）。

---

*本报告基于 2026-09-23 的代码快照分析；所有行号引用对应当时的 `F:\SmartShapeCrop` 源码。取代 V1.0（`ProductSummary/项目审查报告/综合形状_中间挖洞+L形挖角_实现方案分析_V1.0_20260915.md`）与早期短报告版本，作为唯一决策依据。*
