# SmartShapeCrop（智能裁剪设计器 V2.1）近两日工作总结

> 覆盖 2026-08-27 ~ 2026-08-28；8/26 完成全量代码审查与 F1–F3 修复，作为本系列的开端一并简述。

## 一、用户反馈 Bug 修复

### 1. 水池设计器"两侧黑色背景框"（core/image_ops.py）
- **现象**：`65x115CM` 裁剪素材导出图两侧出现宽黑色背景框，`59x117cm` 正常。
- **根因**：等比 contain 后画布左右延展大，`_edge_extend_fill` 用素材最外 1px 边缘像素平铺延展；素材自带黑色外框被拉伸成数百像素黑带。
- **修复**：新增 `_estimate_outer_border` 检测实心外框；延展 > 画布边 5% 且存在外框时，改用内容区中心像素平铺，避开外框与抗锯齿接缝；小延展/无外框保持原"薄边框"行为。
- **验证**：复现脚本黑带 100% → 0%，无外框素材不崩溃，端到端水池渲染黑占比 0%。

### 2. 塞纳时光圆角裁剪"米黄色弧形缺口"（core/corner/sector_render.py）
- **根因**：`is_near_border_color` 遍历的色集含间隙层自身颜色，距离为 0 导致 `force_clear_gap` 永不触发；且旧清除逻辑只匹配"检测到的间隙色"，JPEG/缩放色偏后残留像素连成米黄弧线。
- **修复**：改用实心边框色集判定，使清除分支正确触发；清除逻辑改为保留边框色与背景、清除间隙深度带内其余像素（严格条件才进入，不误伤装饰/图案间隙）。
- **验证**：修复前 1826 个残留像素 → 修复后 0；全套件回归通过。

## 二、V2.1 回归：26 个失败测试全部修复

- **根因四类**：① 10 个测试自身路径 Bug（V2.1 移动 tests/ 目录后 PROJECT_ROOT 未同步）；② 9 个 F1 真实回归（RectShape 归一化/mask 崩溃守卫丢失）；③ 6 个 F4 回归（CropDesign.clone() 不存在）；④ 1 个 F9 warning 缺失。
- **修复**：`core/geometry.py` 恢复全部退化守卫并新增 `CropDesign.clone()`（deepcopy + 共享缓存图）；修正 integration 测试路径。
- **结果**：202/26/5 → **228 passed / 0 failed / 5 skipped（97.9%）**。

## 三、F5–F19 风险项收尾（行为中性，不改功能逻辑）

| 项 | 内容 |
|---|---|
| F5 | OCR 不可用文案澄清（无几何降级路径，需自装 Tesseract） |
| F6 | OCR 真实超时：4 处 `image_to_data` 加 20s timeout + 7 步法 deadline 总闸门 |
| F7 | 删除死代码 `_fill_corner_boundary_pixels`（全仓无调用） |
| F8–F9 | inverse 行为测试锁定；椭圆+边框组合显式 warning |
| F10 | 依赖改 `opencv-python-headless`（避免 Qt 冲突） |
| F11 | 删除陈旧 `智能裁剪设计器V2.0.spec` |
| F12 | `process_image.py` 硬编码路径 → argparse 参数化 |
| F13 | "简单缩放"文案改为"简单缩放（拉伸填满）" |
| F14 | sketch_parser 47 处裸 except 全部补日志（AST 扫描归零） |
| F15 | 调试脚本混入 tests/ 治理（conftest 排除名单双向校验） |
| F16 | PreviewRenderWorker 生命周期：retire + shutdown，杜绝运行中析构 |
| F17 | pytest.ini 移除全局 DeprecationWarning 忽略 |
| F18 | log_setup 文档对齐代码（默认 INFO） |
| F19 | 新增 `core/artifact_cleanup.py`：30 天 + 单目录 200 上限白名单治理，启动自动执行 |

## 四、工程治理（8/28）

### 1. 目录膨胀治理
- 项目 **1,029MB → 794MB**（不含 .venv：547 → 312MB），.git **277MB → 40MB**。
- 删除 build/（270MB）；移除 233 个调试文件 git 跟踪（logs/debug_output/scripts/logs/crash.log），保留 F19 保护名单日志与 dist exe。
- 误放 .git 的 237MB 个人数据（微信图片+安装包）移出并清理；.gitignore 固化新增忽略规则。

### 2. 巨型模块 Facade 薄壳拆分（对外 API 与文件路径不变）
| 模块 | 拆分前 | 拆分后 |
|---|---|---|
| sketch_parser.py | 2,692 行 | facade 572 + base/cache/vision/numbers/margins 5 子模块 |
| image_cropper.py | 1,757 行 | facade 429 + mask 1,016 + border 445 |
| property_panel.py | 1,822 行 | facade 360 + 6 子模块，主类拆 3 个 Mixin |

- 拆分脚本：`scripts/split_sketch_parser.py`、`scripts/split_image_cropper.py`（及 property_panel 对应脚本）。
- 回归护栏：233 用例基线不变（228/0/5）；3 个 .bak 备份保留作回滚源（用户已确认）。

## 五、测试与交付物

- **测试全绿**：228 passed / 5 skipped（约 17–37s），覆盖核心算法、各修复项回归、拆分护栏。
- **三份可视化报告同步至 V2.1.x**：
  - `SmartShapeCrop_项目分析报告_V2.1.html`（根目录主报告）
  - `ProductSummary/SmartShapeCrop_分析报告.html`（F1–F19 风险报告）
  - `ProductSummary/SmartShapeCrop_V2.1_项目分析报告.html`（全景总结报告）
- 8/26 风险报告已导入资料库：https://www.workbuddy.cn/space/d/MM18ZWTylCfTzwVneZilaF

## 当前状态与遗留

- V2.1 功能完整、测试全绿、工程结构健康（无巨型模块、无膨胀、无静默吞错）。
- 遗留事项：3 个 .bak 拆分前备份已保留，待用户最终决定是否删除。
