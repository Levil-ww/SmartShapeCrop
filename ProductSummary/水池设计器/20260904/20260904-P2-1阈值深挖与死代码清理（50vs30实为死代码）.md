# 2026-09-04 T4：P2-1 阈值深挖与死代码清理

## 概述

用户追问 50 vs 30 是什么参数、处理哪个模块。深挖后**推翻"有意差异"的旧判断**——50 实为死代码里的孤立参数。

***

## 两个参数的真实归属

| 参数 | 位置 | 用途 | 生产调用 |
|---|---|---|---|
| **30** | `config.py:117 BORDER_BG_SIMILARITY_THRESHOLD` | `core/corner/detection.py` 的 `_detect_border_layers` / `_get_border_layers_robust`（圆角边框层检测），从图片边缘采样、把颜色与 bg_color 欧氏距离 ≤30 的层视为背景过滤 | ✅ **生产路径在用** |
| **50** | `image_cropper.py:216 CROP_BG_SIMILARITY` | `_filter_gap_layers`（image_cropper.py:188）内，过滤智能裁剪中"图片内容/底色被误判成的间隙层" | ❌ **生产代码零调用**，仅被 `scripts/split_image_cropper.py`（规划清单）与归档脚本引用 |

> `image_cropper.py:210` 注释称对齐 `sector_render.py` 的 `GAP_COLOR_DIST`，但该常量**根本不存在**（grep 为空），是腐烂注释。

***

## 结论

50 vs 30 不是"活跃参数冲突"，而是**死代码里的孤立参数**。生产路径只跑 30。

P2-1 定性从「需业务确认阈值」**降级为「清死代码**（`_filter_gap_layers` 连同 50 一起删）」。

***

## 清理动作

1. **删除 `_filter_gap_layers`**（image_cropper.py:188-226，含 CROP_BG_SIMILARITY=50）——41 行死代码，image_cropper.py 436→395 行
2. **`git rm scripts/split_image_cropper.py`**——已执行完的一次性拆分脚本，且 GROUPS 清单还引用着已删除的 `_clear_inner_arc_to_bg`，已与当前代码脱节

***

## 验证

- compileall 通过
- 全量测试 299 passed / 5 skipped 无回归
- core/gui/main 无残留引用

***

## 意外事件：scripts/ 目录被整体删除（已恢复）

清理过程中发现 scripts/ 目录 230 个文件在工作区被整体删除（**非本次清理范围，疑似 IDE 误操作**）。reflog 显示用户 11:26 自行提交了报告（8815859，只含 3 文件），此后 scripts/ 被删。

经用户确认后 `git restore scripts/` 无损恢复 229 个文件（split_image_cropper.py 保持删除）。

***

## 遗留提示

scripts/ 下另有 `split_property_panel.py`、`split_sketch_parser.py` 两个同类一次性拆分脚本，性质相同但未引用已删死代码（未脱节），本次未动，是否清理待用户决定。
