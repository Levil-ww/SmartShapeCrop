# 2026-09-05 T1：V2.2 全面只读体检

## 概述

用户要求对当前项目做「V2.2 全面检测」。全程只读，未改动任何源码/配置/版本库。

产出：`ProductSummary/SmartShapeCrop分析报告/SmartShapeCrop-V2.2-全面检测报告.html`（54.6 KB，六维体检）

***

## 检测口径与关键数据

- 代码基线 `57efd0a`，生产代码 **89 个 py / 32,800 行**（core 15,739 / tests 7,261 / gui 6,271），注释率 11.1%
- 扫描需**排除 `.venv/`**（在项目根目录内，含 4,264 个 py 文件，不排除会让统计完全失真）
- `scripts/_archive/` 另有 161 个归档 py，需与生产代码分开统计
- 测试实跑：**299 passed / 5 skipped / 0 failed**，53.76s，退出码 0

***

## 核心发现（按严重度）

| 严重级 | 发现 | 详情 |
|---|---|---|
| **P0** | 打包断点 | 打包配置停在 V2.1.2，白名单**不含 V2.2 新增的 `core/lshape_border.py`（887 行）**。该模块由 `core/image_ops.py:1040` **函数内延迟导入**，属 PyInstaller 高风险漏收场景——漏收时只在用户触发 L 形挖角时才炸，测试期发现不了 |
| **P0** | 17 个失效断言 | 17 个测试用 `return <bool>` 而非 `assert`（pytest 不校验返回值 = 假通过）。全在 `tests/border/` 与 `tests/integration/`，正是验证历史 bug 修复的回归测试 |
| **P1** | tests/gui 空壳 | `tests/gui/test_gui_sim.py` 无 `test_` 函数，顶层代码在 pytest **收集阶段**就执行真实 OCR，且依赖的 `scripts/diagnose/_test_sketch1.png` 已不存在。当前靠 `parse_sketch` 返回 `False` 才没炸，未来若改抛异常会导致 304 个测试全部无法运行。GUI 层 6,271 行**零覆盖** |
| **P1** | 仓库污染 | `.workbuddy/` 54.4 MB（PyQt5 wheel 备份）**已被 git 跟踪**且 .gitignore 未覆盖。注意：`.gitignore` 对已跟踪文件无效，必须 `git rm --cached` 才生效 |
| **P1** | 未使用导入 424 处 | 集中于 `property_panel_*` 六件套与 `image_cropper`/`sketch_parser` 拆分残留（大文件拆分时整块复制 import 未裁剪） |
| **P1** | scipy 冗余 | venv 装有 scipy 1.18 但全项目零引用；`PyInstaller` 未写入 requirements |
| **P2** | README 过时 | 标题仍 V2.1；目录树声称根目录有 `packageV2.1.2.py`（9/4 已移入 `packaging/`）；完全未提 `core/lshape_border.py` |
| **P2** | 其他 | 40 处 `except Exception: pass` 静默吞异常；6 处 print 调试残留；25 个高复杂度函数（最重 195 分支/709 行）；项目 929 MB 而源码净重仅 8 MB |

***

## 环境侧注意事项（踩坑记录）

- **`pytest` 退出码 1 是环境干扰，不是测试失败**：pytest 收尾清理历史临时目录（119 个文件）时触发环境的批量删除保护。用 `--basetemp` 指定独立临时根目录即可拿到干净的退出码 0
- 统计测试分布不能靠肉眼数 `.`：用自定义 pytest 插件（`-p plugin`，需 `PYTHONPATH` 指向插件目录）在 `pytest_collection_modifyitems` 里按 `it.fspath` 归类，结果才准确
- 精确分布：core 145 / integration 77 / sketch 67 / border 15 / gui 0 = 304

***

## 待确认事项

仓库瘦身（`git rm --cached .workbuddy -r`）与批量清导入涉及删除/退跟踪，按既定流程**先备份、待确认后再动手**，本次未执行。
