# SmartShapeCrop 综合形状功能可行性分析与实现报告

## 1. 需求映射

示意图可表达为：画布 185×88 cm；中心矩形洞 80×60 cm，左/右/上/下边距分别为 45/60/10/18 cm；外轮廓在一个角位形成标准单边 L 形。L 形角位允许左上（tl）、左下（bl）、右上（tr）、右下（br）。中心洞支持空白和素材填充两种语义。

## 2. 现有架构与可行性

项目已经具备完整的实现链路：

- `core.geometry.CropDesign` 保存画布、四边距、L 形角位及宽高，并提供参数校验和像素换算。
- `build_lshape_mask`、`compute_lshape_border_bands` 负责四角 L 形掩膜和边框带。
- `core.image_ops.render_design` 将外框素材、L 形缺口、中心洞和洞内素材组合渲染。
- `workers.property_panel_workers.PoolRenderWorker` 将识别/手工参数转换为 `rect_lshape` 设计。
- `gui.lshape_panel.LShapePanel` 已提供四角选择、标准 L 形模式和参数编辑；`property_panel_poolbox` 已提供“空白/素材填充”选择。

因此无需新增依赖，也无需更换数据模型。现有实现对“标准单边 L 形 + 中间挖洞”的核心流程可直接复用；图示尺寸只需写入四边距和 L 形参数即可。

## 3. 空白与填充语义

- 空白：`pool_hole_transparent=True`，中心洞和 L 缺口以纯白表达，适合 JPG 输出中的挖空语义。
- 填充：`pool_hole_transparent=False` 并提供 `pool_inner_material_image`，中心洞按洞矩形尺寸适配素材；若未提供素材则使用 `hole_bg_color`。
- L 形外框素材始终保留在保留区，缺口区域按上述空白/填充语义处理。

## 4. 验证结果

已运行现有回归集：

```text
tests/integration/test_pool_lshape_flow.py
tests/core/test_lshape_rendering.py
24 passed
```

测试覆盖四个角位、空白、洞色填充、素材填充、Worker 数据流以及原有矩形挖洞回归。pytest 结束阶段因 Windows 临时目录权限返回清理异常，但测试主体均已通过；这与项目 `AGENTS.md` 记录的环境问题一致。

## 5. 风险与后续建议

当前 `rect_lshape` 的历史语义是“外框与内洞共用 L 形掩膜”。若要严格复现示意图中“外轮廓为 L、中心洞保持矩形”的差异，应新增一个兼容字段（例如 `inner_hole_shape='rect'|'lshape'`）并在 UI 中显式选择，默认保持旧行为。该扩展不需要第三方依赖，预计涉及 `CropDesign`、掩膜构造、边框带计算、UI 快照和回归测试五处，属于下一步增强，不应隐式改变现有项目输出。

## 6. 结论

本阶段目标在现有能力范围内可行，四个方位、中心空白和中心素材填充均已有稳定实现与测试保障。示意图尺寸可直接进入现有参数面板使用；若要求中心洞必须保持矩形，应按第 5 节增加显式形状选项后再启用。
