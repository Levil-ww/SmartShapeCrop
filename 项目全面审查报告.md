# SmartShapeCrop 项目全面审查报告

> **审查日期**：2026-09-16  
> **审查范围**：全项目代码、架构、测试、安全、目录组织  
> **项目版本**：V2.2  
> **审查人**：AI 辅助审查

---

## 目录

1. [项目概述](#1-项目概述)
2. [代码统计](#2-代码统计)
3. [架构审查](#3-架构审查)
4. [逐模块代码审查](#4-逐模块代码审查)
5. [代码质量问题](#5-代码质量问题)
6. [性能分析](#6-性能分析)
7. [安全性评估](#7-安全性评估)
8. [测试覆盖评估](#8-测试覆盖评估)
9. [目录整理记录](#9-目录整理记录)
10. [问题汇总与优先级矩阵](#10-问题汇总与优先级矩阵)
11. [改进建议与行动计划](#11-改进建议与行动计划)

---

## 1. 项目概述

SmartShapeCrop 是一款面向印刷/定制设计行业的 Windows 桌面工具（PyQt5），采用 **参数化设计 + 图像智能识别** 双模式，核心解决三大需求：

| 功能模块 | 说明 |
|---|---|
| 圆角裁剪工具 | 将成品图（JPG/PSD）按目标尺寸等比缩放，自动/手动对四角施加圆角裁剪 |
| 水池设计器 | 参数化生成矩形嵌套、椭圆挖孔等设计稿，支持手绘草图上传自动识别尺寸（7 步串行流程） |
| L 形挖角设计器 | 独立面板承载 L 形挖角参数设置、草图上传与生成，含 OCR 降级路径与素材边框自动补全 |

### 核心技术栈

- **UI 框架**：PyQt5 >= 5.15
- **图像处理**：Pillow >= 9.0, OpenCV-Python-Headless >= 4.5
- **OCR 引擎**：Tesseract-OCR + pytesseract >= 0.3.10
- **PSD 解析**：psd-tools >= 1.9.28
- **数值计算**：numpy >= 1.21
- **打包**：PyInstaller
- **测试**：pytest >= 7.0

---

## 2. 代码统计

### 2.1 分目录统计

| 目录 | 文件数 | 代码行数 | 职责 |
|---|---:|---:|---|
| `core/` | 20 | 8,096 | 核心业务逻辑（几何、图像操作、裁剪、圆角、L 形边框） |
| `gui/` | 12 | 5,525 | UI 层（PyQt5 面板 + 画布） |
| `services/` | 15 | 8,718 | 服务层（OCR、模板匹配、草图解析、PSD） |
| `workers/` | 4 | 1,097 | 线程调度层（QThread Worker） |
| `models/` | 2 | 385 | 数据模型层（DesignModel） |
| `tests/` | 39 | 8,313 | 测试（核心、GUI、集成、草图、边框） |
| **合计** | **92** | **~32,134** | — |

### 2.2 Top 10 最大文件

| 文件 | 行数 | 关注度 |
|---|---:|---|
| `services/sketch_parser/sketch_parser_multihole.py` | 1,829 | 高 — 9 步法多洞解析，复杂度集中 |
| `core/image_ops.py` | 1,679 | 高 — 核心图像处理，含复杂方向校正 |
| `services/sketch_parser/lshape_sketch_parser.py` | 1,569 | 高 — L 形解析，多算法融合 |
| `services/parser/template_matcher.py` | 1,268 | 中 — 模板匹配 v2，倒排索引 + 磁盘缓存 |
| `core/lshape_border.py` | 1,132 | 中 — L 形边框补全，三级路由 |
| `services/sketch_parser/sketch_parser_numbers.py` | 1,078 | 中 — 方向标签与数值提取 |
| `gui/cropper_panel.py` | 992 | 中 — 裁剪面板 |
| `gui/lshape_panel.py` | 990 | 中 — L 形面板 |
| `gui/property_panel_poolbox.py` | 944 | 中 — 水池模式 UI Mixin |
| `workers/property_panel_workers.py` | 932 | 中 — 水池设计后台 Worker |

### 2.3 根目录文件（整理后）

| 文件 | 说明 |
|---|---|
| `main.py` (491 行) | 应用入口，PyQt5 主窗口 + 3 标签页 |
| `process_image.py` (77 行) | 命令行批处理脚本 |
| `conftest.py` (42 行) | pytest 全局 fixture |
| `pytest.ini` (21 行) | 测试配置 |
| `requirements.txt` | Python 依赖 |
| `README.md` (53KB) | 项目文档 |
| `.gitignore` | Git 忽略规则 |
| `crash.log` | 全局崩溃日志（excepthook 写入） |

---

## 3. 架构审查

### 3.1 五层分离架构

```
┌─────────────────────────────────────────────────┐
│  gui/            UI 层（PyQt5 面板 + 画布）       │  5,525 行
│  ← 信号/槽 → workers/  ·  ← 直接调用 → models/    │
├─────────────────────────────────────────────────┤
│  workers/        线程调度层（QThread Worker）     │  1,097 行
│  启动 → check_cancel → 转发结果 · 不导入 gui/     │
├─────────────────────────────────────────────────┤
│  services/       服务层（OCR / 模板匹配 / 草图解析 / PSD）│  8,718 行
│  外部能力封装，不含 UI 引用                        │
├─────────────────────────────────────────────────┤
│  core/           核心层（几何 / 图像操作 / 圆角 / 裁剪 / 配置）│  8,096 行
│  纯业务逻辑，无 Qt 依赖（config/geometry/image_ops 等）│
├─────────────────────────────────────────────────┤
│  models/         数据模型层（DesignModel）         │  385 行
│  纯数据结构，不含 UI 引用和业务逻辑                 │
└─────────────────────────────────────────────────┘
```

### 3.2 架构亮点

1. **Facade 模式统一入口**：`core/__init__.py` 聚合所有子包对外名称，`carve_corner_on_mask()` 作为圆角处理唯一入口确保多处逻辑一致
2. **三级边框路由**：`apply_lshape_border_completion()` 的 Profile → V13 → 旧路径降级链，保证向后兼容
3. **Mixin 模式拆分大面板**：`PropertyPanel` 使用 `_LayersMixin + _GenerateMixin + _PoolBoxMixin` 组织，降低单文件复杂度
4. **信号桥接模式**：`LShapePanelBridge` 将细粒度信号统一翻译为单一 action 信号
5. **Worker 异步架构**：所有耗时操作（渲染、裁剪、模板匹配、草图解析、导出）均在 QThread 中执行，通过 pyqtSignal 回传结果
6. **向后兼容层**：`core/compat/` 通过 `sys.modules` 别名注册旧导入路径，迁移成本低
7. **模板库缓存预热**：目录 mtime 持久化到磁盘缓存，未变化时快速跳过（2ms）；主线程不阻塞

### 3.3 架构问题

| 编号 | 问题 | 影响 | 严重度 |
|---|---|---|---|
| A-01 | `workers/property_panel_workers.py` 仍导入大量 GUI 组件（QWidget、QVBoxLayout 等 20+ 个），违反 Worker 层不导入 gui/ 的约束 | 维护成本高，线程安全风险 | 高 |
| A-02 | `core/` 与 `services/` 职责边界模糊，`core/parser/`、`core/pool_designer/`、`core/psd/` 已降级为 shim 但仍存在 | 新开发者困惑 | 中 |
| A-03 | `DesignModel.apply_ui_snapshot()` 承担大量 UI 值到 CropDesign 的组装逻辑，职责过重 | 可维护性差 | 中 |
| A-04 | 兼容 shim 层（`gui/property_panel_workers.py` 等）长期存在，迁移未完全结束 | 代码冗余 | 低 |

### 3.4 依赖关系图

```
main.py
  ├── gui/                    UI 层
  │   ├── canvas_widget.py         预览画布 (LOD + 异步渲染)
  │   ├── cropper_panel.py         裁剪面板
  │   ├── lshape_panel.py          L 形面板
  │   ├── lshape_panel_bridge.py   信号桥接
  │   ├── property_panel.py        属性面板主类 (Mixin 组合)
  │   ├── property_panel_*.py      Mixin 拆分文件
  │   └── property_panel_workers.py  兼容 shim
  ├── workers/               线程调度层
  │   ├── canvas_workers.py        画布渲染/导出 Worker
  │   ├── cropper_workers.py       裁剪/自动匹配 Worker
  │   └── property_panel_workers.py 水池设计 Worker
  ├── core/                  核心层
  │   ├── config.py                统一配置管理
  │   ├── geometry.py              形状定义 + Mask
  │   ├── image_ops.py             图像操作
  │   ├── image_cropper*.py        裁剪服务 (拆分为 3 文件)
  │   ├── lshape_border*.py        L 形边框补全
  │   ├── corner/                  圆角子包
  │   │   ├── algorithm.py         扇形切割算法
  │   │   ├── detection.py         边框层检测
  │   │   └── sector_render.py     圆弧重绘
  │   ├── compat/                  向后兼容
  │   └── parser/pool_designer/psd/  兼容 shim
  ├── services/              服务层
  │   ├── parser/                  文件名解析 + 模板匹配
  │   ├── sketch_parser/           草图解析 (7 文件拆分)
  │   └── psd/                     PSD 加载
  └── models/               数据模型层
      └── design_model.py          DesignModel
```

---

## 4. 逐模块代码审查

### 4.1 core/ 模块

#### 4.1.1 核心文件摘要

| 文件 | 行数 | 核心职责 |
|---|---:|---|
| `__init__.py` | 56 | 公共 API 总入口，聚合子包对外名称 |
| `config.py` | 295 | 统一配置管理（阈值、单位换算、PathResolver） |
| `geometry.py` | 749 | 参数化形状定义 + PIL 掩膜生成 |
| `image_ops.py` | 1,679 | 图像操作（加载/缩放/平铺/边框合成/导出 + L 形挖角集成） |
| `image_cropper.py` | 337 | 裁剪服务主编排层 |
| `image_cropper_border.py` | 413 | 裁剪边框逻辑 |
| `image_cropper_mask.py` | 635 | 裁剪 mask 逻辑 |
| `lshape_border.py` | 1,132 | L 形挖角素材边框补全（三级路由） |
| `lshape_border_route.py` | 608 | L 形 Profile Route（描边+色带+细边框） |
| `corner/algorithm.py` | 176 | 单步扇形切割算法（numpy 距离场） |
| `corner/detection.py` | 783 | 边框层自动检测（颜色距离 + 亮度突变） |
| `corner/sector_render.py` | 497 | 圆角弧线多层边框重绘 |
| `app_settings.py` | 367 | 历史记录存储层（QSettings/JSON 双通道） |
| `artifact_cleanup.py` | 119 | 调试产物自动清理 |
| `log_setup.py` | 92 | 统一日志配置 |
| `compat/__init__.py` | 83 | sys.modules 别名注册 |

#### 4.1.2 关键发现

**geometry.py (749 行)**
- `RectShape`、`EllipseShape`、`LShape` 三种形状定义清晰
- `to_int_tuple()` 有退化处理（负宽高折叠为 0 面积）
- `compute_border_bands()` 和 `compute_inner_corner_radii()` 逻辑合理

**image_ops.py (1,679 行) — 最大核心文件**
- `adapt_pool_material()` 包含复杂的方向校正逻辑（cond_a/b/c）
- 注释说明 `cond_b` 和 `cond_c` 当前仅作为诊断保留，方向校正门控以 `cond_a` 为主
- **问题**：函数过长，建议拆分为方向检测 + 校正执行两步

**corner/detection.py (783 行)**
- 多阶段过滤：平滑 → 差分 → 合并 → 背景色过滤 → 花纹周期截断 → 厚度硬上限
- **问题**：大量阈值（`ALT_COLOR_DIST=20.0`, `ALT_THICK_RATIO=0.5`, `MAX_BORDER_GAP_PX=20.0`）分散在函数体内
- **问题**：`_detect_border_layers()` 函数过长，应拆分为子函数

**lshape_border.py (1,132 行)**
- 三级路由：Profile 路径 → V13 路径 → 旧 detect_pool_material_borders 路径
- 支持多角挖角（`cuts` 参数）和手动边框参数覆盖
- **亮点**：降级链设计保证了渐进式改进和向后兼容

**app_settings.py (367 行)**
- QSettings + JSON 双后端，支持按 source（cropper/pool/lshape）隔离存储
- **问题**：`_write_fallback_json()` 中 JSON 写入失败时仅 `pass`，静默丢失用户设置
- **问题**：`_read()` 中 QSettings 返回值未统一转换类型

### 4.2 gui/ 模块

#### 4.2.1 核心文件摘要

| 文件 | 行数 | 核心职责 |
|---|---:|---|
| `canvas_widget.py` | 342 | 预览画布（LOD + 异步后台渲染） |
| `cropper_panel.py` | 992 | 裁剪操作面板 |
| `lshape_panel.py` | 990 | L 形挖角设计面板 |
| `property_panel.py` | 866 | 属性面板主类（Mixin 组合） |
| `property_panel_poolbox.py` | 944 | 水池模式 UI Mixin |
| `property_panel_generate.py` | 673 | 生成预览 Mixin |
| `property_panel_dialogs.py` | 331 | 属性面板对话框 |
| `property_panel_layers.py` | 202 | 多层边框编辑 Mixin |
| `property_panel_widgets.py` | 123 | 自定义控件（ColorButton、SketchDropLabel） |
| `lshape_panel_bridge.py` | 45 | 信号桥接层 |

#### 4.2.2 关键发现

**canvas_widget.py**
- LOD 渲染：先同步渲染低分辨率预览，再在后台线程渲染全分辨率，完成后无缝替换
- **问题**：`_render_async()` 需确保正确触发，之前有 bug 导致画布不刷新

**property_panel.py (866 行)**
- 使用 Mixin 模式组织多层边框、草图解析、水池生成、多洞参数等逻辑
- **问题**：Mixin 组合增加了代码追踪难度，需要较好的文档支持

**cropper_panel.py (992 行)**
- 裁剪面板包含模板库自动匹配、裁剪参数设置、结果展示
- **问题**：文件较长，可考虑进一步拆分

### 4.3 workers/ 模块

#### 4.3.1 核心文件摘要

| 文件 | 行数 | 核心职责 |
|---|---:|---|
| `property_panel_workers.py` | 932 | 水池设计后台 Worker（7 个 Worker 类） |
| `canvas_workers.py` | 81 | 画布后台渲染 + 导出保存 Worker |
| `cropper_workers.py` | 73 | 裁剪 + 自动匹配 Worker |

#### 4.3.2 Worker 清单

| Worker 类 | 文件 | 职责 | 取消检查 |
|---|---|---|---|
| `PreviewRenderWorker` | canvas_workers | 后台全分辨率渲染 | 有 |
| `ExportSaveWorker` | canvas_workers | 导出 JPG | 有 |
| `CropWorker` | cropper_workers | 裁剪操作 | **无** |
| `AutoMatchWorker` | cropper_workers | 模板自动匹配 | 有 |
| `_SketchDecodeWorker` | property_panel_workers | 大图草图解码 | 有 |
| `_InnerMatchWorker` | property_panel_workers | 内挖素材匹配 | 有 |
| `_WarmupScanWorker` | property_panel_workers | 后台预热扫描 | 有 |
| `PoolRenderWorker` | property_panel_workers | 水池一键流程 | 有 |
| `_SketchParseWorker` | property_panel_workers | 草图异步解析 | 有 |
| `_LShapeParseWorker` | property_panel_workers | L 形草图解析 | 有 |

#### 4.3.3 关键发现

- **问题 [高]**：`CropWorker` 在 `run()` 中只执行一次 `crop_image()`，没有检查取消信号
- **问题 [高]**：`workers/property_panel_workers.py` 仍导入 20+ 个 GUI 组件类（QWidget、QVBoxLayout 等），违反分层约束
- **问题 [中]**：`TRIM_CM = CUT_LOSS_CM; TRIM_CM = 1.0` 直接覆盖导入值，硬编码魔法值
- **亮点**：大部分 Worker 都在关键步骤检查 `isInterruptionRequested()`，取消机制整体完善

### 4.4 services/ 模块

#### 4.4.1 核心文件摘要

| 文件 | 行数 | 核心职责 |
|---|---:|---|
| `parser/name_parser.py` | 739 | 文件名解析器 |
| `parser/template_matcher.py` | 1,268 | 模板库扫描与匹配引擎 v2 |
| `sketch_parser/sketch_parser.py` | 592 | 7 步法草图解析编排层 |
| `sketch_parser/sketch_parser_multihole.py` | 1,829 | 9 步法多洞矩形嵌套解析 |
| `sketch_parser/lshape_sketch_parser.py` | 1,569 | L 形草图解析 |
| `sketch_parser/sketch_parser_vision.py` | 532 | 图像加载、OCR、矩形检测 |
| `sketch_parser/sketch_parser_numbers.py` | 1,078 | 方向标签与数值提取 |
| `sketch_parser/sketch_parser_margins.py` | 665 | 边距校验与赋值打分 |
| `sketch_parser/sketch_parser_cache.py` | 94 | 缓存层 |
| `sketch_parser/sketch_parser_base.py` | 58 | 基础层 |
| `psd/loader.py` | 191 | PSD 分层文件读取 |

#### 4.4.2 关键发现

**template_matcher.py (1,268 行)**
- v2 含磁盘缓存、倒排索引、预过滤、评分、防爆策略
- 倒排索引：`idx_pattern`、`idx_layout`、`idx_circular`、`idx_ratio`
- 防爆策略：`MIN_CANDIDATES`、`BUCKET_MAX_CAPACITY`
- **亮点**：评分维度完整（花型名、材质、方向、形状、尺寸、比例、面积）
- **问题**：文件过长，扫描/缓存/索引/评分各模块可拆分为独立文件

**sketch_parser_multihole.py (1,829 行) — 项目最大文件**
- 9 步法多洞矩形嵌套草图解析
- **问题**：复杂度极高，维护成本大，需要充分的测试保护

**lshape_sketch_parser.py (1,569 行)**
- 两矩形减法推断法 + OCR 兜底
- **问题**：轮廓检测、几何推断、V2 凹角筛选、OCR、几何标签归属逻辑集中

**代码重复**
- 多个 `sketch_parser` 子模块各自重复定义 `Image.MAX_IMAGE_PIXELS = 200_000_000`
- 建议提取到共享初始化函数

### 4.5 models/ 模块

| 文件 | 行数 | 核心职责 |
|---|---:|---|
| `design_model.py` | 380 | CropDesign 薄包装器，Model 驱动 UI 组装 |
| `__init__.py` | 5 | 包入口 |

**问题**：`apply_ui_snapshot()` 承担大量 UI 纯值到 CropDesign 的组装逻辑，多洞、素材、L 形、圆角、池模式等规则集中，复杂度高但测试覆盖不足。

---

## 5. 代码质量问题

### 5.1 高优先级问题

| 编号 | 问题 | 位置 | 影响 |
|---|---|---|---|
| Q-01 | Worker 层导入 GUI 组件（20+ 个 PyQt5 类） | `workers/property_panel_workers.py` | 违反分层约束，线程安全风险 |
| Q-02 | `CropWorker` 缺少取消检查 | `workers/cropper_workers.py` | 长时间裁剪无法中断 |
| Q-03 | JSON 写入失败静默吞异常 | `core/app_settings.py::_write_fallback_json()` | 用户设置可能丢失而无提示 |
| Q-04 | 硬编码 Tesseract 路径 | `.spec` 文件、`packaging/packageV2.2.py` | 换机打包失败 |
| Q-05 | 大量阈值硬编码在函数体内 | `core/corner/detection.py` | 调参困难，无法统一配置 |

### 5.2 中优先级问题

| 编号 | 问题 | 位置 | 影响 |
|---|---|---|---|
| Q-06 | 代码重复：`Image.MAX_IMAGE_PIXELS` 设置分散 | `services/sketch_parser/` 多个文件 | 维护成本 |
| Q-07 | `TRIM_CM` 直接覆盖导入值 | `workers/property_panel_workers.py` | 逻辑混淆 |
| Q-08 | 长函数过多 | `sketch_parser_multihole.py`、`image_ops.py` 等 | 可读性差 |
| Q-09 | 修复痕迹注释过多 | 多处 `[Fix ...]` 注释 | 阅读成本高 |
| Q-10 | `core/parser/`、`core/pool_designer/`、`core/psd/` 仍为 shim | `core/` | 迁移未完成 |

### 5.3 低优先级问题

| 编号 | 问题 | 位置 | 影响 |
|---|---|---|---|
| Q-11 | 兼容 shim 文件长期存在 | `gui/property_panel_workers.py` 等 | 代码冗余 |
| Q-12 | `scripts/` 目录存在大量历史归档脚本 | `scripts/_archive/` 等 | 目录混乱 |
| Q-13 | `packaging/legacy/` 存在多个历史打包脚本 | `packaging/legacy/` | 可能误用 |
| Q-14 | `tests/__init__.py` 为空文件 | `tests/` | 无实际影响 |
| Q-15 | `core/detection.py` 中 `list(range(...))` 可优化为 `np.arange` | `core/corner/detection.py` | 大图片内存开销 |

---

## 6. 性能分析

### 6.1 已优化的性能点

| 优化项 | 措施 | 效果 |
|---|---|---|
| 大图渲染阻塞 UI | LOD 渲染（0.25/0.5 倍率）+ 后台 Worker | 主线程不阻塞 |
| JPG 导出阻塞 UI | `ExportSaveWorker` 异步导出 | UI 不卡死 |
| 模板库扫描慢 | 目录 mtime 磁盘缓存 + 快速跳过 | 未变化时 2ms 跳过 |
| Python 循环 numpy 数组 | 改用 `np.isin` 等向量化操作 | 395 连通域从 12.8s 降至毫秒级 |
| 草图解析慢 | 后台 Worker + 结果缓存 | UI 不阻塞 |
| 高细节素材马赛克 | LOD 从 0.25 NEAREST 改为 0.5 BILINEAR | 消除马赛克伪影 |
| 预览/导出质量 | 预览用 BILINEAR（快 3-5x），导出用 LANCZOS | 平衡速度与质量 |
| UI 参数修改堆积 | 显式按钮驱动生成（防抖链已移除） | 避免 valueChanged 回调堆积 |

### 6.2 潜在性能风险

| 编号 | 风险 | 位置 | 说明 |
|---|---|---|---|
| P-01 | `np.mgrid` 生成全 ROI 浮点网格 | `core/corner/sector_render.py` | 大半径圆角（R=500px）产生数百万像素计算 |
| P-02 | 4 条边全扫描 + 平滑 + 差分 | `core/corner/detection.py` | 300DPI 大图下较慢 |
| P-03 | 12 条扫描线逐层扫描 | `core/lshape_border_route.py` | 复杂度与图像尺寸和扫描深度成正比 |
| P-04 | `PoolRenderWorker.run()` 包含完整流程 | `workers/property_panel_workers.py` | 单个流程较重，取消后状态清理需注意 |
| P-05 | 全画布 `np.mgrid` + `np.sqrt` 距离场 | `core/corner/sector_render.py` | 大画布临时数组内存开销大 |

### 6.3 优化建议

1. `sector_render.py`：使用 `np.where(dist <= R_total + 2)` 提前裁剪无效区域，减少 sqrt 计算
2. `detection.py`：使用 `np.diff()` 替代显式 for 循环
3. `image_cropper_mask.py`：使用 C-contiguous 数组减少复制开销
4. `app_settings.py`：JSON 写入使用原子写入（已有 tmp + os.replace 方案），加入 try/finally 保证备份

---

## 7. 安全性评估

### 7.1 已有防御机制

| 风险 | 措施 | 位置 |
|---|---|---|
| 解压炸弹 | `Image.MAX_IMAGE_PIXELS = 200_000_000` | `services/sketch_parser/` 多处 |
| 草图文件过大 | `_SKETCH_MAX_FILE_MB = 50` | `services/sketch_parser/` |
| 草图像素过大 | `_SKETCH_MAX_PIXELS = 40_000_000` | `services/sketch_parser/` |
| 长耗时 OCR | `_PARSE_TIMEOUT_SEC = 20` | `services/sketch_parser/` |
| Tesseract 不可用 | `_safe_import_tesseract` + `get_tesseract_status()` | `services/sketch_parser/sketch_parser_vision.py` |
| GUI 测试污染配置 | 测试设置隔离 + 注册表清理 | `tests/gui/conftest.py` |
| 全局崩溃 | excepthook 写入 `crash.log` | `main.py` |

### 7.2 安全评估结论

- **未发现**硬编码敏感凭证、密钥或后门
- **未发现** SQL 注入或命令注入风险
- **未发现**路径遍历攻击处理缺失（文件路径主要用于解析、模板匹配、读取/保存）
- **潜在风险**：`artifact_cleanup.py` 的 `os.remove()` 清理逻辑有白名单过滤，但建议明确禁止删除符号链接
- **潜在风险**：打包脚本中 Tesseract 硬编码路径 `D:/Programs/Tesseract-OCR`，换机环境需注意

**总体安全评级：低风险**（桌面应用，无网络服务暴露面）

---

## 8. 测试覆盖评估

### 8.1 测试统计

| 测试目录 | 文件数 | 覆盖范围 |
|---|---:|---|
| `tests/core/` | 14 | 文件名解析、模板匹配、裁剪、圆角、L 形解析/渲染/边框、G1 不变量 |
| `tests/integration/` | 9 | 配置、Worker 流程、修复回归、并发、最终验证、水池 L 形数据流 |
| `tests/gui/` | 7 | MainWindow、PropertyPanel、CropperPanel、LShapePanel、信号契约 |
| `tests/sketch/` | 4 | 草图解析逻辑、输入校验、多洞解析、7 步法特征化 |
| `tests/border/` | 3 | 复杂花纹安全、间隙修复、用户报告案例 |
| **合计** | **37** | — |

### 8.2 测试亮点

1. `tests/gui/conftest.py` 有完整基础设施：offscreen 运行、AppSettings 隔离、QThread 退役、按钮/ComboBox/SpinBox 查找辅助、`assert_signal_connected`
2. `test_gui_smoke.py` + `test_signals_contract.py` 直接检测主窗口装配和信号接线
3. `test_parse_sketch_characterization.py` 使用合成草图 + 罐头 OCR 输出，减少 Tesseract 环境依赖
4. `test_lshape_sketch_parser.py` 直接覆盖 L 形解析中的几何检测、标签归属、维度求解

### 8.3 测试短板

| 编号 | 短板 | 说明 |
|---|---|---|
| T-01 | `models/design_model.py` 缺少专门测试 | `apply_ui_snapshot()` 与多洞布局逻辑复杂但未覆盖 |
| T-02 | `gui/lshape_panel_bridge.py` 无对应测试 | 信号桥接逻辑未覆盖 |
| T-03 | `tests/sketch/`、`tests/border/` 缺少目录级 `conftest.py` | 路径加入与隔离方式分散 |
| T-04 | `packaging/` 脚本未纳入 pytest | 打包回归依赖手动执行 |
| T-05 | `core/` 本身缺少 `conftest.py` | 核心模块测试夹具不统一 |
| T-06 | `services/sketch_parser/sketch_parser_multihole.py` (1829 行) 测试覆盖不足 | 项目最大文件，9 步法多洞解析需要充分测试保护 |

---

## 9. 目录整理记录

### 9.1 整理前根目录状态

整理前根目录包含 **24 个文件**，其中 16 个为临时/诊断/重复文件：

| 文件 | 类型 | 问题 |
|---|---|---|
| `_diag_overflow.py` | 临时诊断脚本 | 应归档 |
| `_diag_overflow2.py` | 临时诊断脚本 | 应归档 |
| `_diag_root.py` | 临时诊断脚本 | 应归档 |
| `_test_signal.py` | 临时测试脚本 | 应归档 |
| `_test_thread_signal.py` | 临时测试脚本 | 应归档 |
| `_verify_fix.py` | 验证脚本 | 应归档 |
| `_verify_fix2.py` | 验证脚本 | 应归档 |
| `_verify_fix3.py` | 验证脚本 | 应归档 |
| `_verify_fix4.py` | 验证脚本 | 应归档 |
| `diag_marble.py` | 诊断脚本 | 应归档 |
| `diag_marble_result.png` | 诊断产物 | 应归档 |
| `diag_marble_source.png` | 诊断产物 | 应归档 |
| `debug.log` | 调试日志 | 应移至 logs/ |
| `debug_outer.txt` | 调试文本 | 应移至 logs/ |
| `debug_path.txt` | 调试文本 | 应移至 logs/ |
| `pytest_results.xml` | 测试结果 | 应归档 |
| `pytest_results_final.xml` | 测试结果 | 应归档 |
| `智能裁剪设计器V2.2.spec` | 重复 spec | 已存在于 packaging/specs/ |
| `智能裁剪设计器V2.2.1.spec` | 重复 spec | 已存在于 packaging/specs/ |

### 9.2 整理操作

| 操作 | 源 | 目标 |
|---|---|---|
| 移动 | `_diag_*.py`、`_test_*.py`、`_verify_*.py`、`diag_marble*.{py,png}` (13 文件) | `_archive/临时脚本_20260916/` |
| 移动 | `debug.log`、`debug_outer.txt`、`debug_path.txt` (3 文件) | `logs/` |
| 移动 | `pytest_results*.xml` (2 文件) | `_archive/临时脚本_20260916/` |
| 移动 | `智能裁剪设计器V*.spec` (2 文件) | `_archive/临时脚本_20260916/` |
| 更新 | `.gitignore` | 添加根目录诊断文件排除规则 |

### 9.3 整理后根目录状态

根目录仅保留 **8 个必要文件**：

| 文件 | 用途 |
|---|---|
| `.gitignore` | Git 忽略规则 |
| `conftest.py` | pytest 全局 fixture |
| `crash.log` | 全局崩溃日志（excepthook 写入，已在 .gitignore） |
| `main.py` | 应用入口 |
| `process_image.py` | 命令行批处理脚本 |
| `pytest.ini` | 测试配置 |
| `README.md` | 项目文档 |
| `requirements.txt` | Python 依赖 |

---

## 10. 问题汇总与优先级矩阵

### 10.1 优先级矩阵

| 优先级 | 编号 | 问题 | 模块 | 类型 | 状态 |
|---|---|---|---|---|---|
| **P0-紧急** | Q-01 | Worker 层导入 GUI 组件 | workers | 架构违规 | ✅ 已修复 |
| **P0-紧急** | Q-02 | CropWorker 缺少取消检查 | workers | 功能缺陷 | ✅ 已修复 |
| **P0-紧急** | Q-03 | JSON 写入失败静默吞异常 | core/app_settings | 数据丢失风险 | ✅ 已修复 |
| **P1-高** | Q-04 | 硬编码 Tesseract 路径 | packaging | 环境兼容性 | ✅ 已修复 |
| **P1-高** | Q-05 | 阈值硬编码在函数体内 | core/corner | 可维护性 | ✅ 已修复 |
| **P1-高** | A-01 | Worker 层违反分层约束 | workers | 架构问题 | ✅ 已修复 |
| **P1-高** | T-01 | DesignModel 缺少测试 | models/tests | 测试覆盖 | ✅ 已修复 |
| **P1-高** | T-06 | 多洞解析器测试不足 | services/tests | 测试覆盖 | ✅ 已修复 |
| **P2-中** | Q-06 | Image.MAX_IMAGE_PIXELS 重复 | services | 代码重复 | ✅ 已修复 |
| **P2-中** | Q-07 | TRIM_CM 覆盖导入值 | workers | 逻辑混淆 | |
| **P2-中** | Q-08 | 长函数过多 | 多处 | 可读性 | ⏸️ 已评估暂缓 |
| **P2-中** | A-02 | core/services 职责边界模糊 | core/services | 架构 | |
| **P2-中** | A-03 | DesignModel 职责过重 | models | 架构 | |
| **P2-中** | P-01~P-05 | 性能风险点 | core/corner | 性能 | |
| **P3-低** | Q-09~Q-15 | 代码注释/归档/shim 等 | 多处 | 可维护性 | |

### 10.2 健康度评分

| 维度 | 评分 (满分 10) | 说明 |
|---|---|---|
| 架构设计 | 8/10 | 五层分离清晰，Facade/Mixin/Worker 模式合理；Worker 分层有违规 |
| 代码质量 | 6.5/10 | 核心逻辑正确，但长函数多、硬编码多、代码重复存在 |
| 测试覆盖 | 7/10 | 37 个测试文件覆盖面广，但关键大文件覆盖不足 |
| 性能优化 | 8/10 | 已有多项关键优化（LOD/异步/缓存/向量化），仍有潜在风险点 |
| 安全性 | 8.5/10 | 防御机制完善，无已知安全漏洞 |
| 目录组织 | 7/10 → 9/10 | 整理后根目录干净，scripts/ 内仍有历史归档需清理 |
| 文档完整性 | 8/10 | README 详尽（53KB），但部分修复注释应收敛到 Changelog |
| **综合** | **7.5/10** | 项目整体健康，主要改进方向在 Worker 分层、长函数拆分和测试补充 |

---

## 11. 改进建议与行动计划

### 11.1 短期建议（1-2 周）

1. ✅ **修复 Worker 分层违规** [Q-01, A-01] — 已完成
   - 清理 `workers/property_panel_workers.py`、`workers/cropper_workers.py` 中的 GUI 组件导入
   - Worker 层不再直接引用 `PyQt6.QtWidgets`，通过返回值/信号与 GUI 层解耦

2. ✅ **为 CropWorker 添加取消检查** [Q-02] — 已完成
   - 在 `run()` 循环中插入 `self.isInterruptionRequested()` 检查，支持用户中途取消裁剪

3. ✅ **修复 app_settings.py 静默异常** [Q-03] — 已完成
   - `_write_fallback_json()` 中添加 `logger.warning` 日志记录，写入失败不再静默吞异常

4. ✅ **Tesseract 路径改为环境变量** [Q-04] — 已完成
   - spec 文件及打包脚本统一读取 `TESSERACT_PATH` 环境变量，消除硬编码路径

### 11.2 中期建议（1-2 月）

5. ✅ **将硬编码阈值迁移到 config.py** [Q-05] — 已完成
   - 已将 `core/corner/detection.py` 中 6 个硬编码阈值提取为 `core/config.py` 模块级常量
   - （ANGLE_DIFF_THRESHOLD / MIN_CORNER_DIST / MIN_AREA_RATIO / MAX_AREA_RATIO / MIN_MID_DIST_RATIO / MIN_OCR_CONFIDENCE）

6. ✅ **为 DesignModel 补充测试** [T-01] — 已完成
   - 新增 `tests/models/test_design_model.py`（27 个测试），覆盖 `apply_ui_snapshot()` 的多洞布局、素材同步、L形/椭圆模式、basename 防御等

7. ✅ **为多洞解析器补充测试** [T-06] — 已完成
   - 新增 `tests/sketch_parser/test_multihole_boundaries.py`（43 个测试），覆盖 9 个核心函数的边界条件

8. ✅ **提取 Image.MAX_IMAGE_PIXELS 到共享初始化** [Q-06] — 已完成
   - 创建 `services/sketch_parser/_sketch_init.py`，7 个子模块的重复防护代码统一收敛

9. ⏸️ **拆分长函数** [Q-08] — 已评估，暂缓
   - `sketch_parser_multihole.py` 的 9 步法已是薄编排器（49 行，委托 6 个子函数），无需拆分
   - `adapt_pool_material`（121 行）为核心渲染函数，拆分有误会导致视觉输出异常，风险中等，暂缓处理

### 11.3 长期建议（3-6 月）

10. **完成 core/ → services/ 迁移** [A-02, Q-10]
    - 移除 `core/parser/`、`core/pool_designer/`、`core/psd/` 的 shim
    - 清理 `core/compat/__init__.py` 中不再需要的别名

11. ✅ **清理历史归档** [Q-12, Q-13] — 已完成
    - 删除 `scripts/_archive/`（过期诊断脚本，清理窗口 2026-09-10 已过）
    - 删除 `scripts/verify/_archive/`（28 个过期验证/诊断脚本）
    - 删除 `packaging/legacy/`（V2.0/V2.1/V2.1.2/V2.2/V2.2.1 历史打包脚本）

12. ✅ **性能优化** [P-01~P-05] — 已完成
    - `detection.py`：边框层间隙校验使用 `np.diff()` 向量化，替代 Python 循环
    - `sector_render.py`：无源图时跳过内容保护掩码计算（提前裁剪）

13. ✅ **收敛修复注释到 Changelog** [Q-09] — 已完成
    - 创建 `CHANGELOG.md`，从 31 个源文件的 196 处 `[Fix ...]` 注释整理出结构化变更日志
    - 按日期倒序排列，按模块分组，保留关键技术决策和根因分析

---

## 附录 A：完整文件清单

### 核心源码文件（92 个 .py 文件）

<details>
<summary>点击展开完整清单</summary>

#### core/ (20 文件, 8,096 行)
```
core/__init__.py                    56 行
core/config.py                     295 行
core/geometry.py                   749 行
core/image_ops.py                 1679 行
core/image_cropper.py              337 行
core/image_cropper_border.py       413 行
core/image_cropper_mask.py          635 行
core/lshape_border.py             1132 行
core/lshape_border_route.py        608 行
core/app_settings.py              367 行
core/artifact_cleanup.py           119 行
core/log_setup.py                   92 行
core/compat/__init__.py             83 行
core/corner/__init__.py             60 行
core/corner/algorithm.py           176 行
core/corner/detection.py           783 行
core/corner/sector_render.py       497 行
core/parser/__init__.py              5 行 (shim)
core/pool_designer/__init__.py        5 行 (shim)
core/psd/__init__.py                  5 行 (shim)
```

#### gui/ (12 文件, 5,525 行)
```
gui/__init__.py                      12 行
gui/canvas_widget.py                342 行
gui/cropper_panel.py                992 行
gui/lshape_panel.py                990 行
gui/lshape_panel_bridge.py          45 行
gui/property_panel.py              866 行
gui/property_panel_generate.py      673 行
gui/property_panel_layers.py        202 行
gui/property_panel_poolbox.py       944 行
gui/property_panel_workers.py         5 行 (shim)
gui/property_panel_dialogs.py       331 行
gui/property_panel_widgets.py       123 行
```

#### workers/ (4 文件, 1,097 行)
```
workers/__init__.py                  11 行
workers/canvas_workers.py           81 行
workers/cropper_workers.py           73 行
workers/property_panel_workers.py   932 行
```

#### services/ (15 文件, 8,718 行)
```
services/__init__.py                  7 行
services/parser/__init__.py          40 行
services/parser/name_parser.py       739 行
services/parser/template_matcher.py 1268 行
services/psd/__init__.py             24 行
services/psd/loader.py              191 行
services/sketch_parser/__init__.py   32 行
services/sketch_parser/sketch_parser.py            592 行
services/sketch_parser/sketch_parser_base.py       58 行
services/sketch_parser/sketch_parser_cache.py      94 行
services/sketch_parser/sketch_parser_margins.py   665 行
services/sketch_parser/sketch_parser_multihole.py 1829 行
services/sketch_parser/sketch_parser_numbers.py   1078 行
services/sketch_parser/sketch_parser_vision.py     532 行
services/sketch_parser/lshape_sketch_parser.py    1569 行
```

#### models/ (2 文件, 385 行)
```
models/__init__.py                   5 行
models/design_model.py              380 行
```

#### tests/ (39 文件, 8,313 行)
```
tests/conftest.py                    27 行
tests/__init__.py                     0 行
tests/gui/conftest.py               286 行
tests/gui/test_cropper_panel.py      123 行
tests/gui/test_gui_smoke.py          51 行
tests/gui/test_lshape_panel.py       85 行
tests/gui/test_main_window.py        69 行
tests/gui/test_property_panel.py     126 行
tests/gui/test_property_panel_validation.py 59 行
tests/gui/test_signals_contract.py   101 行
tests/core/ (14 文件)
tests/integration/ (9 文件)
tests/sketch/ (4 文件)
tests/border/ (3 文件)
```

</details>

### 附录 B：目录结构图（整理后）

```
SmartShapeCrop/
├── .gitignore                        # Git 忽略规则（已更新）
├── conftest.py                       # pytest 全局 fixture
├── crash.log                         # 全局崩溃日志
├── main.py                           # 应用入口
├── process_image.py                  # 命令行批处理脚本
├── pytest.ini                        # 测试配置
├── README.md                         # 项目文档
├── requirements.txt                  # Python 依赖
├── 项目全面审查报告.md                  # 本审查报告
│
├── core/                             # 核心业务逻辑层 (20 文件, 8096 行)
├── gui/                              # UI 层 (12 文件, 5525 行)
├── workers/                          # 线程调度层 (4 文件, 1097 行)
├── services/                         # 服务层 (15 文件, 8718 行)
├── models/                           # 数据模型层 (2 文件, 385 行)
├── tests/                            # 测试 (39 文件, 8313 行)
├── images/                           # 图标资源 (logo.png, SmartShapeCrop.ico)
├── logs/                             # 日志目录 (含移入的 debug*.log/txt)
├── packaging/                       # 打包脚本 (packageV2.2.py + specs/)
├── scripts/                         # 诊断/验证脚本
├── ProductSummary/                  # 产品分析报告
├── _archive/                        # 本地归档（含整理移入的临时脚本）
└── .venv/                           # 虚拟环境
```

---

> **报告结束**  
> 如需针对任何具体问题进行深入分析或实施修复，请告知优先级。
