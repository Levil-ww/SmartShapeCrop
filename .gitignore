# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/

# 虚拟环境
venv/
.venv/
env/
ENV/

# IDE 配置
.idea/
.vscode/

# 环境变量
.env
.env.*

# 系统文件
.DS_Store
Thumbs.db
# 调试产物（F19 治理：仅本地保留，不进入版本库）
logs/
debug_output/
scripts/logs/
/crash.log
/simple_corner_test.png
/test_corner_result.png
# 根目录临时诊断脚本和产物（应归档到 _archive/ 或 scripts/diagnose/）
/diag_*.py
/diag_*.png
/_diag_*.py
/_test_*.py
/_verify_*.py
/debug*.log
/debug*.txt
/pytest_results*.xml
# 根目录 .spec 副本（正式 spec 在 packaging/specs/）
/智能裁剪设计器V*.spec

# 本地归档（2026-09-02 整理：调试图片与 .bak 备份，仅本地保留）
_archive/

# 本地工具与归档（2026-09-24 治理）
# 根因：以下三个目录曾各有文件被误跟踪（.workbuddy 55 / .dumate 15 /
# .trae-html-share-packages 1，合计 71），而 .gitignore 从未覆盖它们。
# ⚠️ .gitignore 只对「未跟踪」文件生效 —— 已跟踪文件必须先
# `git rm -r --cached .workbuddy .dumate .trae-html-share-packages` 才能解除。
/.workbuddy/
/.dumate/
/.trae-html-share-packages/
# Python 测试遗留（09-19 QA 起未覆盖；.pytest_cache 另有其自带 .gitignore）
/.pytest_tmp/
/.pytest_cache/
