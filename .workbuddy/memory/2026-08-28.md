# 2026-08-28

- 完成 SmartShapeCrop 项目全面分析（架构/代码规模/测试实测/风险），生成可视化报告：`D:/SmartShapeCrop/SmartShapeCrop_项目分析报告.html`
- 关键数据：核心代码 14,616 行；26 个测试文件 233 用例，实测 202 通过 / 26 失败 / 5 跳过（失败全在 integration 回归护栏，部分因 requirements.txt 为空及重构后 API 变化）
- 发现的真实问题：requirements.txt 为空；OCR 硬依赖 Tesseract；sketch_parser.py(2667行)/property_panel.py(1822行)/image_cropper.py(1757行) 巨型模块
- 测试环境：系统 Python（C:/Users/Administrator/AppData/Local/Programs/Python/Python313）装有项目依赖，托管 Python 无 pytest/PyQt5
- 报告更新为 V2.1：标题/页脚标注版本 V2.1，文件重命名为 `SmartShapeCrop_项目分析报告_V2.1.html`

### 复核：requirements.txt 更新（10:20-10:40）
- 确认根目录 requirements.txt 存在且内容完整（git 跟踪：2a42c17/f853283/4b08494），F2/F10 断言全部满足；之前"缺失/为空"判断系误判
- 26 个 integration 测试失败根因彻底定位：
  1) 10 个 = 测试自身路径 Bug：V2.1 提交把 tests/*.py 移入 tests/{border,core,gui,integration,sketch}/（git R100 重命名），但测试内 PROJECT_ROOT 解析（`.parent.parent` 或 `dirname(__file__)+'..'`）未同步，去 tests/ 下找根目录文件
  2) 9 个 F1 = 真实回归：RectShape 归一化/mask 崩溃守卫在 V2.1 丢失，PIL ValueError
  3) 6 个 F4 = 真实回归：CropDesign.clone() 已不存在（AttributeError）
  4) 1 个 F9 = 椭圆边框 warning 缺失
- 三份报告全部更新（主报告内联修正 + ProductSummary 两份加"2026-08-28 复核更新说明"横幅）：
  - SmartShapeCrop_项目分析报告_V2.1.html（根目录）
  - ProductSummary/SmartShapeCrop_分析报告.html
  - ProductSummary/SmartShapeCrop_V2.1_项目分析报告.html

### 修复全部 26 个失败测试（10:25-10:45）
- 全量测试从 202/26/5 → **228 通过 / 0 失败 / 5 跳过**（97.9%）
- 修改文件：
  1) tests/integration/test_f5_f14_fixes.py + test_f15_f19_fixes.py：PROJECT_ROOT 由 .parent.parent 改为 .parent.parent.parent（+ test_f2_f3_fixes.py 的 req 路径加一级）；F5 参数 packageV2.0.py → packageV2.1.py
  2) core/geometry.py：新增 logger + copy 导入；RectShape.to_int_tuple 负宽高折叠为零面积；outer/inner_rect_px clamp 非负；fill_rect_mask/fill_ellipse_mask/apply_rounded_corners_to_mask 退化守卫；新增 CropDesign.clone()（deepcopy + 共享 _cached_outer_image）；compute_border_bands 加椭圆+边框 warning（F9）
  3) core/pool_designer/sketch_parser.py：3 处 except ValueError: pass → logger.debug（F14）
- 生产代码 gui/canvas_widget.py:141 已用 self._design.clone()，F4 并发防护完整
- 三份报告测试数据更新为 228/0/5（主报告 V2.1.2 修订 + ProductSummary 两份横幅/页脚）

### 修复目录膨胀（11:00-11:15）
- 扫描发现膨胀源：build/ 270MB、dist/ 245MB、logs/ 57MB（129 个被 git 跟踪）、debug_output/ 11MB（91 个被跟踪）、scripts/logs/ 4MB、pycache；**意外发现 .git/My Pictures 237MB**（微信图片+安装包误放入 .git，git 零引用）
- 处理（用户确认：保留 exe、移除 git 跟踪）：
  1. rm -rf build/（270MB，git 已忽略）
  2. git rm -r --cached logs debug_output scripts/logs crash.log（232 文件移除跟踪），保留 smartshapecrop.log* 与 crash.log（artifact_cleanup 保护名单）
  3. 删除 logs 调试截图/ debug_output/ scripts/logs/ 工作区文件
  4. 清理项目内全部 __pycache__ 与 .pyc（不含 .venv）
  5. **.git/My Pictures 移到 D:\_rescued_My_Pictures_误放_20260828**（个人数据不删除，供用户处置）
  6. .gitignore 追加 logs/ debug_output/ scripts/logs/ /crash.log
- 效果：项目 1029MB → 794MB（不含 .venv：547 → 312MB）；.git 277MB → 40MB；git 索引移除 233 个文件
- 测试验证：228 passed / 5 skipped，与清理前一致，无副作用
