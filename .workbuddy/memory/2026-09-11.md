# 2026-09-11 工作日志

## 项目全面检测（六维体检）— 产出 HTML 报告

用 `python-project-health-audit` skill 对 SmartShapeCrop V2.2 做全量只读扫描。
报告：`ProductSummary/SmartShapeCrop分析报告/SmartShapeCrop-V2.2-全面检测报告-20260911.html`
健康度 66/100，全程零写操作（源码未动）。

### 核心结论

- 测试：379 收集 / 374 passed / 5 skipped / 0 failed，退出码 0，耗时 68s
- 打包白名单 42/42 全覆盖，**0 漏收、0 僵尸条目**（维护得很好）
- 无硬编码密钥；README 与 V2.2 同步
- 主要问题：发布链路（PyInstaller 未装 + bat 路径失效 + exe 过期）与仓库卫生（69MB 二进制误跟踪）

### 关键数据（供后续对比）

| 维度 | 值 |
|---|---|
| PY 文件 | 266（源码206/测试57/归档3），源码 46,879 行，注释率 19.7% |
| 编译错误 | 源码 0；8 处全在 scripts/**/_archive（BOM U+FEFF） |
| 未使用导入 | 587，集中在 gui/property_panel_*.py（拆分复制残留） |
| broad except | 232；bare except 15（全在 _archive） |
| 超长函数>80行 | 98；最长 render_design 816 行 |
| 高复杂度>20 | 63；最高 _extract_direction_label_numbers cx=180 |
| 大类 | 6；最大 CropperPanel 1030 行/48 方法 |
| 延迟导入 | 247 处（PyInstaller 漏收高发区，当前靠白名单人工兜住） |
| 用例分布 | core 220 / integration 77 / sketch 67 / border 15 / **gui 0** |
| 磁盘 | 总计 781MB；.git 149MB / build 213MB / dist 203MB / scripts 85MB |

### 踩坑与口径（重要）

1. **项目解释器 = 系统 Python 3.13.14**
   `C:\Users\Administrator\AppData\Local\Programs\Python\Python313\python.exe`
   （managed 3.13.12 没有 PyQt5/pytest，别用错）

2. **git 大文件扫描不要用 bash+xargs+stat**，385 个文件会跑到超时被 SIGTERM。
   改用 python 子进程调 `git ls-files` + `os.path.getsize`，秒出。

3. **空壳测试文件的判定**：`grep -c "^\s*def test_"` 为 0 但文件名匹配 `test_*.py`。
   本例 5 个共 552 行：test_gap_detail_analysis / test_corner_analysis_simple /
   test_diagnose / test_sketch_fix / test_verify。它们不贡献用例但收集期会 import
   并执行顶层代码 —— 是套件级定时炸弹。

4. **README 测试数据已过期**：写「320 passed」，实测 374。以后别在文档写死数字。

5. **`.gitignore` 对已跟踪文件无效**这条又应验了两次：
   - `.workbuddy/`（47 文件，含 47.76MB wheel）与 `.dumate/`（13 文件）完全不在 .gitignore 里，且已跟踪
   - `_archive/` 虽在 .gitignore，但仍有 215 个文件被跟踪（主要在 scripts/_archive/）
   必须 `git rm -r --cached` 才生效。**南烛尚未确认，本次未执行。**

6. **扫描期间 HEAD 会变**：开始是 acc5bf3，跑完变 9b710fe（南烛在并行提交）。
   dist exe 09:30 构建，落后于 10:54 的提交 —— 产物过期结论由此得出。
   写报告前记得重新取一次 HEAD。

## 后续：5 个空壳文件移出 tests/（已执行完成）

南烛确认后执行。**结果：379 收集 / 374 passed 不变，零回归。**

| 原位置 | 新位置 |
|---|---|
| tests/border/test_gap_detail_analysis.py | scripts/diagnose/_diag_border_gap_detail.py |
| tests/core/test_corner_analysis_simple.py | scripts/diagnose/_diag_corner_analysis_simple.py |
| tests/sketch/test_diagnose.py | scripts/diagnose/_diag_sketch_recognition.py |
| tests/sketch/test_sketch_fix.py | scripts/diagnose/_diag_sketch_fix.py |
| tests/sketch/test_verify.py | scripts/diagnose/_diag_sketch_verify.py |

配套改动：`tests/conftest.py` 的 `collect_ignore` 清空（保留空列表 + 变更记录）；
README 目录树移除 test_corner_analysis_simple.py，并把「320 passed」改为 374。

### ⚠️ 重要纠偏（我上一节报错了风险等级）

**这 5 个文件早就被 `tests/conftest.py` 的 `collect_ignore` 屏蔽了** —— pytest 根本
不会 import 它们，所以「未来抛异常导致整个套件无法收集」的风险**已经解除**，
应定级 P2（位置不对、误导读者），不是 P1 定时炸弹。

我上一节的判断来自静态扫描（5 个文件 0 个 test_ 函数），没去查 conftest 就下了结论。
**教训：定级前必须先查 `grep -rn collect_ignore conftest.py tests/conftest.py`。**
已把这条写进 `python-project-health-audit` 技能的第 7 条陷阱。

### 移动操作的标准范式（项目内已有先例，务必沿用）

`scripts/diagnose/_gui_sim_diag.py` 就是同一个先例（原 tests/gui/test_gui_sim.py，
2026-09-05 移出）。范式要点：

1. `git mv` 保留重命名历史
2. 下划线前缀 `_diag_` 不匹配 `test_*.py` —— 双保险
3. **必须替换 `sys.path.insert(0, '.')`**：原写法依赖 cwd，换目录后失效。
   改为 `PROJECT_ROOT = Path(__file__).resolve().parents[2]` + `sys.path.insert(0, str(PROJECT_ROOT))`
   （scripts/diagnose/x.py 的 parents[2] 才是项目根）
4. 顶部 docstring 按既有格式记录原路径 + 移出原因 + 运行方式
5. `collect_ignore` 移除条目并在注释里追加变更记录
6. 验证：收集总数与通过率必须与移动前**完全一致**

注意：这 5 个脚本依赖的草图素材（`scripts/diagnose/_test_sketch1.png` 等）**本来就已缺失**，
移动前同样跑不通，非本次引入的回归。

### 提交记录

已提交：**`28c8ab5` v2.2-将5个诊断脚本移出tests目录并产出V2.2全面检测报告**
（9 files changed, 1032 insertions, 22 deletions；5 个 rename + 3 处修改 + 2 个新增）
工作区提交后干净。提交信息沿用项目 `v2.2-<描述>` 惯例，正文分 5 点记录改动理由。

## 失效测试清理扫描（已出报告，等待确认）

报告：`ProductSummary/SmartShapeCrop分析报告/SmartShapeCrop-V2.2-失效测试清理报告-20260911.html`
九类失效模式只读扫描，tests/ 28 文件 / 347 个 test 函数。**未执行任何清理，等南烛勾选范围。**

### 结论：测试体系比预想健康

命中 0 的类别：空壳文件 0（上轮已清）、空函数体 0、同名覆盖 0、过期引用 0、
非 Test* 类内 test_ 方法 0、永久跳过 0、恒真断言 0。

### 需处理 6 项

1. **P1 假测试**：`tests/border/test_gap_fix_verification.py:239` — 59 行，算出
   `content_preserved = blue_count > total_pixels * 0.9` 却只 print 不 assert，永远绿灯
2. **P1 5 个用例从未执行**：`tests/border/test_border_fix.py` 全部 5 个依赖
   `psd_demo/双面格-定制-定制尺寸-简织;竖版54x41.2cm.jpg`。
   **全项目连 psd_demo/ 目录都没有**（只有 .dumate/inbox 下同系列其他产品图），
   这 5 个自写入起一直跳过
3. **P2** `test_lshape_render.py:49/:69` 只 `print('  PASSED')` 无断言
4. **P2** `test_image_cropper.py:145 test_no_corners` 唯一断言是 `assert result is not None`
5. **P2** `scripts/verify/test_multilayer_debug.py` / `test_xianxu_debug.py` 命名违规
   （0 test 函数却叫 test_*.py），应改 `_verify_*_debug.py`
6. **P2** `tests/gui/` 空目录（README 标注预留，建议不动）

### 踩坑：两处必须人工复核，否则严重误报

1. **弱断言 25 处 → 真实只有 1 处**。`assert x is not None` 多数是合理的空值前置检查，
   后面还跟着真断言。必须判断「弱断言是否为该函数的**唯一**断言」。
   只有 `test_image_cropper.py:145` 是唯一的，其余 24 处是误报。
   判断方法：统计函数内 assert 总数 == 1 且匹配 `^\w+ is not None$`。

2. **「缺失文件引用」39 处 → 真实只有 1 组**。两类假阳性：
   - `tmp_path / "src.jpg"` 动态生成，扫描时当然不存在
   - 模板匹配测试里的中文文件名字符串**仅用于解析**，不读盘
   （如 `"双面格-定制-定制尺寸-简织;竖版55x41cm.jpg"`）
   不能只看路径字面量是否存在就判失效。

### 新增检查项（值得保留）

**非 `Test*` 类内的 test_ 方法永不执行** —— `pytest.ini` 的 `python_classes = Test*` 意味着
类名不以 Test 开头时，其下所有 `test_*` 方法都被静默忽略。这是最隐蔽的失效模式，
本项目命中 0。以后扫失效测试务必带上这条。

### 待南烛确认后才动的事项

- [ ] 阶段一：装 pyinstaller/pywin32 + 建 requirements-dev.txt + spec 路径改相对 + 重新打包
- [ ] 阶段二：`cp -r .git .git.bak` → 补 .gitignore → `git rm -r --cached .workbuddy .dumate scripts/_archive`
- [ ] 阶段二延伸：149MB 历史压缩（filter-repo/BFG）—— 风险高，单独确认
- [x] 阶段三（部分）：5 个空壳文件已移出 tests/ ✅ 2026-09-11 完成
- [ ] 阶段三（剩余）：补 psd_demo 源图；GUI 冒烟测试
- [ ] 阶段四：autoflake 清 587 处导入（清理后必须重跑 379 用例）
