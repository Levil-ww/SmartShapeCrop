# 2026-09-01

## L 型挖角功能检测（v2.1）

- 用户请求：检测程序并查询 L 型挖角的实现过程。
- 完成 very thorough 级代码追溯，输出两张图（操作流程 + 实现链路）。
- 实际跑测试 22 条：15 通过 / 7 失败，全部 7 个失败已定位为环境/测试用例问题：
  1. 草图解析 5 个失败 = 本机未装 Tesseract OCR（requirements.txt 明确要求独立安装引擎）。降级路径（target 尺寸+像素比例反推）已验证：corner 检测 4/4 正确，挖角尺寸误差 ≤ 3%。UI 实际场景传了 raw_w/raw_h，可走降级。
  2. 集成测试 2 个失败 = `.venv` 虚拟环境 PyQt5 DLL 加载失败（系统 Python PyQt5 5.15.11 正常）。venv 安装损坏，非代码问题。
- 实际渲染测试 10/10 通过，独立生成 `rect_lshape` 样图，拓扑/圆角/边框带视觉验证通过。

## 关键发现

- L 型挖角在 `_lshape_sketch_parser.py` 与 `geometry.py::build_lshape_mask` 中实现完整。
- 数据结构：`CropDesign.l_corner/l_cut_w_cm/l_cut_h_cm` + `LShape` dataclass。
- 渲染链路：`render_design` → `_get_inner_pixel_mask`(rect_lshape 分支) → `build_lshape_mask` → 边框带 `compute_lshape_border_bands`。
- 重要：用户主环境（系统 Python）正常，但本机 `.venv` 环境的 PyQt5 安装损坏，建议后续若依赖 venv 跑测试需 `pip install --force-reinstall PyQt5==5.15.11` 修复。

## 待跟进

- Tesseract OCR 在主力开发机是否安装？若未装，L 形识别走几何降级路径（需用户在 UI 填目标尺寸），OCR 数字读取不可用。
- `.venv/Lib/site-packages/PyQt5/QtCore.pyd` 加载失败的根因待查（手动 `os.add_dll_directory` 也无法加载，非 PATH 问题）。
