# 2026-09-10 工作日志

## 项目目录整理 —— 只读扫描 + 四档规划（未执行）

南烛要求整理项目目录下的文档文件。用 project-cleanup-planner 技能执行，**首轮严格只读**，
产出 `ProductSummary/SmartShapeCrop分析报告/SmartShapeCrop_项目整理规划_V1.1.html`。
（V1.0 已存在但从未落地，故本次出 V1.1 而非覆盖。）

### 扫描实测数据

- 扫描范围：641 个文件（排除 .venv 457M / .git 119M / .workbuddy 83M / .idea）
- git：master，539 个追踪文件，工作区 4 个未追踪条目

体积大头：
| 项 | 体积 | 处理 |
|---|---|---|
| build/ | 213 MB | PyInstaller 中间产物，可删 |
| dist/智能裁剪设计器V2.2.exe | 203 MB | 今日 15:07 交付物，保留 |
| scripts/_archive | 84 MB | 其中 _diag_20260904_lshape 78 MB |
| logs/ | 7.84 MB | .log.1 5.24 MB 可归档 |
| ProductSummary/ | 2.61 MB（154 文件） | 归类混乱，本次重点 |

### 两个关键发现（V1.0 报告未覆盖）

1. **`.workbuddy/` 81.9 MB 被 git 追踪** —— 这是 `.git` 达 119 MB 的主因。
   明细：wheels_backup 54.3 MB（PyQt5 whl 备份）、tmp_samples 27 MB（e2e/colorfix 临时样本图）。
2. **`.gitignore` 第 37 行 `_archive/` 规则对已追踪文件无效** —— 仍有 94 个误追踪文件 / 88.1 MB 在库内
   （scripts/_archive/test_cropper_output 4.74 MB、scripts/diagnose/_archive/output_images 38 张、
   ProductSummary 配图 1.16 MB）。需 `git rm --cached`。

### 文档归类核心问题

ProductSummary 下**两套正交分类体系平级混放**（这是目录持续变乱的根因）：
- 按模块：水池设计器 86 / 圆角裁剪工具 26 / L形挖角设计器 11 / 分析报告 20
- 按日期：2026-08 (7) / 2026-09 (4) —— 月度横向总结

层级不统一：水池设计器全用日期子目录；圆角裁剪工具、L形挖角设计器是「根目录直放 md + 日期子目录」混合。
另有根目录 2 份审查报告未归口、分析报告目录混装 10 html + 7 png + 3 patch。

### 移动文档前必须同步修的两类引用（本次最大风险点）

1. README.md 第 760 行起「ProductSummary 文档索引」—— 手写 10 处指向 2026-08/2026-09 月度总结的相对链接
2. SmartShapeCrop分析报告/*.html 的 `src="X.png"` 图链 —— 图片移入 assets/ 后需批量改写，否则配图全部失效

已验证：根目录两份审查报告全库无代码/文档引用（仅自查自引），可安全移动。
crash.log 在 main.py + core/artifact_cleanup.py 的保护名单里，属运行时产物，只归档不删除。

### 四档方案与待拍板决策

A 生成物清理(~220MB) / B 文档归类 / C 结构治理 / D Git 瘦身(81.9MB)。
推荐 A+B 一次做完，C 等维度选型，D 单独 commit。

4 个决策点留给南烛选：①ProductSummary 用哪种维度 ②build/ 现在删吗 ③D 档本轮做吗 ④84MB 诊断产物怎么处理。

### ⚠️ 扫描期间观察到的外部改动（非本人所为）

15:25:04 `core/corner/sector_render.py` 被保存，diff 是把 `near_edge` 改成按 corner_key
（tl/tr/bl/br）分别计算 —— 与南烛正在调试的 ring-pattern artifacts bug 相关。
时间在本人扫描窗口内，但本人全程只读未改动该文件，判断是南烛在 PyCharm 侧保存。已在回复中如实说明。

### 备注

- 本轮零移动、零删除（仅新增报告文件一个）
- 扫描脚本暂存于 `C:\Users\Administrator\.workbuddy\tmp\scan_ssc.py` 与 `git_check.py`，清单 JSON 已导出

---

## 第二轮：A+B 档执行完成（15:35 ~ 15:52）

南烛拍板：**A+B，ProductSummary 维度选「甲·模块为主」**（C 档方案就此锁定，本轮未执行其文件移动）。
产出 `ProductSummary/SmartShapeCrop分析报告/SmartShapeCrop_项目整理执行报告_V1.2.html`。

### A 档实测

| 项 | 释放 | 动作 |
|---|---|---|
| build/ | 213 MB | 删（PyInstaller 中间产物） |
| 14 个 __pycache__ | 2.0 MB | 删（.venv 内 39 个排除） |
| .pytest_cache/.pytest_tmp | 0.08 MB | 删（含 39 空目录） |
| logs/smartshapecrop.log.1 | 5.2 MB | 归档进 _archive/logs_20260910/ |
| dist/V2.2.exe | 203 MB | **保留** |

工作区 1.2 GB → 980 MB。

### B 档实测（31 个文件归位）

- 2 份审查报告 → `ProductSummary/SmartShapeCrop分析报告/`
- 该目录分层：7 png → `assets/`，3 patch → `patches/`
- 13 个文件从日期子目录**提平**到模块根（详见下方判断）
- spec → `packaging/specs/`；4 个临时文件 → `_archive/临时脚本_20260910/`
- 1 个异常命名统一：`程序分析与优化-0806.md` → `20260806-程序分析与优化.md`

**图链修正**：7 张配图只被 `SmartShapeCrop-V2.2-回归诊断报告.html` 1 个文件引用，
`src="X.png"` → `src="assets/X.png"`，7/7 全部正确、0 残留、7 处可达。备份已在验证后清理。

### ⭐ 关键经验：日期子目录「提平 vs 下沉」的判断

**偏离了 V1.1 原规划（下沉），改执行提平。** 判断依据值得记住：

触发事实 —— 圆角裁剪工具根目录 16 个 md、**每天只有 1 个**，下沉会造出 16 个单文件碎片目录；
而子目录里的 md **本身已带完整日期前缀**（`20260905-7个回归诊断…md`），提平零信息损失。

决策规则：
1. **文件名已含 YYYYMMDD- 前缀 → 可以提平**，排序天然按时间，可查性不降反升
2. **每天仅 1 个文档 → 绝不建日期子目录**（碎片化）；每天多份kekh才值得下沉
3. **先查 README 有没有记载约定** —— 本项目 README 第 776-778 行已写明
   「圆角裁剪工具按日期命名 / L形挖角按阶段演进 / 水池设计器按日期子目录」。
   提平是**恢复**既有约定，不是发明新规则。**这一步检查能省掉大量主观判断。**

**子目录里的 README 是高质量日度索引，不能删** —— 改名 `<日期>-文档索引.md` 保留全部内容。

### 意外情况

15:26~15:32 南烛自行 commit `f9cd769 V2.2-打包文件`，把 sector_render.py 的 corner_key 修复、
packageV2.2.py、根目录 spec 一并入库。导致：`git diff --stat HEAD -- core gui tests main.py` 从
「有 9 行改动」变为空。**这不是文件丢失**，验证过内容完好（第 352 行改动在）。
教训：**整理过程中若 git 基线变化，要能区分「被还原」和「被提交」，别误判。**

### 四项验证结果

①源码零改动 `git diff --stat HEAD -- core gui tests main.py` 为空 ✓
②`ast.parse` 101 个 .py，0 语法错误 ✓（不用 py_compile，免得 regenerating pyc）
③`.venv` + offscreen 导入 11 个核心模块 11/11 ✓（冒烟生成的 5 个 __pycache__ 已二次清理）
④HTML 配图 7/7 可达，__pycache__ 终检 0 个，core/ 体积 845K 未变 ✓

### 遗留（未做，需南烛决定）

1. **未 commit** —— 工作区现为 29 处删除 + 2 处修改 + 一批 untracked，均为移动所致，无内容丢失。
   提交前建议 `git add -A` 让 Git 识别为 rename。
2. **.gitignore 未补** —— `.workbuddy/` 81.9 MB 仍在库里；本次新移入 assets/ patches/ 的文件同样进了 unruly 区。
   D 档未执行。
3. **C 档甲方案待执行** —— 11 份月度总结移入 `ProductSummary/月度总结/` + README 索引 10 处链接更新。
