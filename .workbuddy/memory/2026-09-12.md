# 2026-09-12 工作日志

## SmartShapeCrop V2.2 全面审查与建议报告（报告 V1.0）

**产物**：`ProductSummary/SmartShapeCrop分析报告/SmartShapeCrop-V2.2-全面审查与建议报告-20260912.html`
（42,655 字符 / 725 行，HTML 标签闭合校验通过）

**审查要求**：南烛明确要求「不能仅根据之前的分析报告，也要结合程序代码进行分析」。
故本次逐条回到代码/日志取证，推翻了昨日报告的一项关键结论。

### 最重要的发现：P0-01 今晨三次真实崩溃（昨日报告称"未复发"）

`crash.log` 中记录 2026-09-12 三次崩溃（09:46:55 / 09:47:12 / 09:59:15），同一根因：

```
main.py:307 in _on_rendered
TypeError: unsupported format string passed to IFDRational.__format__
```

- 根因：`f"{img.info['dpi'][0]:.0f}"` 对 PIL 的 `IFDRational` 类型格式化。该类型**不支持任何格式说明符**（`d`/`f`/`.0f`/`.1f`/`g` 全抛 TypeError），只有空格式可用。
- 触发：上传带 **EXIF 分辨率标记**的图片（手机/相机/微信/PS 保留 EXIF）→ `Image.open()` 出来 dpi 元素即 `IFDRational`。
  两条路径：`main.py:331 _on_sketch_loaded`、`main.py:314 _on_cropped_image`，都把 `Image.open()` 产物 emit 给 `_on_rendered`。
- **项目自有 204 张图的 dpi 全是 int**（程序生成），所以靠项目素材永远复现不出 → 长期潜伏。
- 关键对比：同一项目 `core/image_ops.py:1697-1701`（save_jpg）**已经做了 `int(round(dpi[0]))` 防御**，唯独 main.py:307 漏了。
- 修复口径：与 save_jpg 同源（`float()` / `int(round())` 包裹 + 类型/长度兜底）。
- 探针实证：Qt 槽内异常会被 `sys.excepthook`（main.py:507）写入 crash.log；`_probe_crash.log` 复现出同样内容。

### 测试盲区（P0-03，最值得修的结构性问题）

`tests/gui/test_main_window.py:97` 只用 `assert_signal_connected()` 断言「信号已接线」，
**从未真正传图调用 `_on_rendered`** → 槽函数体零覆盖。433 passed 全绿却拦不住必现崩溃。
结论：GUI 测试的「接线覆盖」做得很好，但「函数体覆盖」是空白。

### 对昨日报告的修订

| 昨日结论 | 实测 |
|---|---|
| crash.log 不存在、崩溃未复发 | ✗ **被证伪**，今晨 3 次 |
| P1-06 三条路由 scale 不一致仍存在 | ✓ **已修**，`lshape_border.py:643-650` 标 `[Fix P1-06 2026-09-12]`，算术平均→几何平均 sqrt(sx*sy) |
| 测试基线 433 passed | ✓ 一致（实测 433 / 84.38s / 退出码 0） |
| 打包链路已跑通 | ⚠ exe 落后源码，且 **.venv 内 pyinstaller 缺失**，无法重新出包 |
| README 漂移已修正 | ⚠ 测试数字仍写 430（三处），且「27 文件·342 用例」自相矛盾 |

**教训**：带「待复验」标记的问题清单腐化很快。今后报告应只保留当天实测状态，
或每条标注取证时间戳。

### 其余实测数据

- 源码：108 文件 / 38,351 行（core 17,755 · tests 8,306 · gui 6,663）
- 未使用导入 **250 处 / 46 文件**（gui 125 · core 96）—— 拆分脚本机械保留 import 头的残留
- 静默吞异常（pass/continue 无日志）**59 处**；bare except 0 处
- 超长函数：render_design 848 行，>150 行共 30 个
- 仓库卫生：`.workbuddy/`、`.dumate/` **未被 .gitignore 覆盖**，51 个文件被跟踪，
  含 47.76 MB 的 PyQt5 whl 备份 + 约 30 MB 测试图
- PyInstaller hiddenimports **40/40 全覆盖**（含 9 处函数内延迟导入）✅
- 测试分布：core 223 / integration 77 / sketch 64 / gui 56 / border 10（收集 430，执行 433，差 3 未定位）
- Tesseract 可用，路径 `D:\Programs\Tesseract-OCR`（非默认位置）
- 健康度评分 64/100（C+），修完 P0 预期升至 82

## 环境踩坑（务必记住，节省下次时间）

1. **Bash 工具在本机不可用** —— `dirname: command not found` / `ls: command not found`，
   PortableGit 的 PATH 未初始化。全部改用 PowerShell 或 Python 脚本。
2. **PowerShell 工具的 stdout 不回传** —— `echo`/`Write-Output` 全部拿不到。
   可靠做法：**让 Python 脚本自己 `write_text()` 落盘，再用 Read 读文件**。
   （PowerShell 的 `*>` 重定向会写成 UTF-16，Read 会报 binary file，不要用。）
3. **PowerShell 跑「内部再起子进程」的脚本时，父进程可能不写盘就退出**（exit 1）。
   本次 `probe_slot.py` 因此三次失败。绕法：让探针脚本自己写产物，父进程只负责读。
   若脚本内部会 abort 子进程（如 Qt 异常探针），改用 `python -c` 直接跑探针。
4. `.venv` 解释器：`F:\SmartShapeCrop\.venv\Scripts\python.exe`（Python 3.13.14，含 PyQt5/PIL）。
   系统 Python 3.13.14 在 `C:\Users\Administrator\AppData\Local\Programs\Python\Python313\`。
   **跑项目测试必须用 .venv 的**。

## 本次未执行的操作（等南烛确认）

按硬性要求，以下均为**只读审查**，未动任何源码或仓库状态：

- ❌ 未修改 `main.py:307`（P0-01 修复）
- ❌ 未补 GUI 槽函数体回归用例（P0-03）
- ❌ 未安装 PyInstaller / 未重新出包（P0-02）
- ❌ 未补 `.gitignore` / 未 `git rm --cached`（P1-03）
- ❌ 未清理根目录重复 spec、调试残留文件、旧打包脚本

中间产物（28 个脚本与原始输出）保留在 `.workbuddy/audit/` 供复核。
